<?xml version="1.0" encoding="utf-8" standalone="yes"?><search><entry><title>22 | 测试、性能与部署：把 API 送上生产</title><url>/blog/rust-22-testing-perf-deploy/</url><categories><category>Rust</category></categories><tags><tag>Rust</tag><tag>axum</tag><tag>测试</tag><tag>Docker</tag><tag>性能优化</tag></tags><content type="html"><![CDATA[  三篇实战项目的最后一篇。前两篇把 task-api 从骨架搭到具备生产级错误处理和中间件，这一篇补上最后三块拼图：测试、性能调优和Docker 部署。
测试 Rust 的测试体系是内置的，不需要额外的测试框架。cargo test 直接运行，编译器帮你检查测试代码的类型安全。
单元测试 单元测试放在被测模块的同一文件里，用 #[cfg(test)] 模块包裹：
// src/store.rs 末尾追加 #[cfg(test)] mod tests { use super::*; use crate::models::{Task, TaskStatus, UpdateTaskRequest}; fn make_store() -&amp;gt; TaskStore { TaskStore::new() } #[test] fn test_create_and_get() { let mut store = make_store(); let task = Task::new(&amp;#34;写测试&amp;#34;.to_string(), None); let id = task.id; store.create(task); let found = store.get(&amp;amp;id).unwrap(); assert_eq!(found.title, &amp;#34;写测试&amp;#34;); assert_eq!(found.status, TaskStatus::Todo); } #[test] fn test_update_task() { let mut store = make_store(); let task = Task::new(&amp;#34;原标题&amp;#34;.to_string(), None); let id = task.id; store.create(task); let req = UpdateTaskRequest { title: Some(&amp;#34;新标题&amp;#34;.to_string()), description: None, status: Some(TaskStatus::Done), }; let updated = store.update(&amp;amp;id, req).unwrap(); assert_eq!(updated.title, &amp;#34;新 …  ]]></content></entry><entry><title>21 | 错误处理与中间件：让 API 更健壮</title><url>/blog/rust-21-error-handling-middleware/</url><categories><category>Rust</category></categories><tags><tag>Rust</tag><tag>axum</tag><tag>tokio</tag><tag>错误处理</tag><tag>中间件</tag><tag>Tower</tag></tags><content type="html"><![CDATA[  上一篇搭起了 task-api 的骨架：路由、Handler、共享状态都到位了。但如果你实际跑一下，会发现一个问题——错误响应太粗糙了。找不到任务只返回 404，没有任何说明；请求体格式不对返回 422，没有告诉前端哪个字段出错。生产 API 不该这样。
这一篇解决两件事：
统一错误处理：自定义错误类型，让所有错误都以一致的 JSON 格式返回 Tower 中间件：接入日志、超时、CORS，以及优雅关闭 统一错误响应格式 先定一个约定：API 的所有错误响应，都用这个格式：
{ &amp;#34;code&amp;#34;: &amp;#34;NOT_FOUND&amp;#34;, &amp;#34;message&amp;#34;: &amp;#34;Task not found&amp;#34; } code 是机器可读的错误码，message 是人类可读的说明。前端可以根据 code 决定展示什么，而不是硬解析 HTTP 状态码。
自定义错误类型 src/errors.rs：
use axum::{ http::StatusCode, response::{IntoResponse, Response}, Json, }; use serde::Serialize; use thiserror::Error; /// 应用错误类型 #[derive(Debug, Error)] pub enum AppError { #[error(&amp;#34;Resource not found: {0}&amp;#34;)] NotFound(String), #[error(&amp;#34;Invalid request: {0}&amp;#34;)] BadRequest(String), #[error(&amp;#34;Internal server error&amp;#34;)] Internal(#[from] anyhow::Error), } /// 统一错误响应体 #[derive(Serialize)] struct ErrorResponse { code: String, message: String, } /// 让 AppError 可以直接作为 axum Handler 的返回值 impl IntoResponse for AppError { fn into_response(self) -&amp;gt; Response { let …  ]]></content></entry><entry><title>20 | 项目搭建与路由设计：用 axum 构建任务管理 API</title><url>/blog/rust-20-axum-project-setup/</url><categories><category>Rust</category></categories><tags><tag>Rust</tag><tag>axum</tag><tag>tokio</tag><tag>serde</tag><tag>REST API</tag></tags><content type="html"><![CDATA[  前面 19 篇，我们把 Rust 的核心概念逐一拆解：所有权、生命周期、Trait、异步、宏、unsafe……现在到了检验的时候——把这些东西放进一个真实项目，看它们如何协同工作。
接下来三篇，我们会从零构建一个任务管理 REST API：
第 20 篇（本篇）：项目搭建、路由设计、请求/响应处理 第 21 篇：统一错误处理、Tower 中间件、优雅关闭 第 22 篇：测试、性能调优、Docker 部署 项目不复杂，但足够覆盖生产级 Rust Web 服务的核心模式。技术栈：axum + tokio + serde + thiserror。
为什么选 axum Rust 的 Web 框架不少：actix-web、axum、warp、poem……为什么选 axum？
axum 是 tokio 团队出品，和 tokio 生态天然契合。它的设计哲学很 Rust：把 HTTP 处理拆成组合式的小块，而不是用宏魔法生成代码。路由、提取器、响应——每一层都是普通的 Rust 类型，编译器全程参与检查。
另一个优势是基于 Tower——一个成熟的中间件生态。日志、超时、限流、CORS，都有现成的 Tower 中间件可以直接插入。
项目结构 先来规划项目结构。一个清晰的结构在项目生长时会省很多麻烦：
task-api/ ├── Cargo.toml └── src/ ├── main.rs # 程序入口，启动服务 ├── routes.rs # 路由注册 ├── handlers.rs # 请求处理函数 ├── models.rs # 数据模型（Task 结构体等） ├── store.rs # 内存存储层 └── errors.rs # 统一错误类型（第 21 篇展开） 这个结构遵循&amp;quot;按职责分层&amp;quot;的原则：数据、业务逻辑、HTTP 层各自独立，便于测试和替换。
初始化项目 cargo new task-api cd task-api Cargo.toml 添加依赖：
[package] name = &amp;#34;task-api&amp;#34; version = &amp;#34;0.1.0&amp;#34; edition = &amp;#34;2021&amp;#34; [dependencies] axum = &amp;#34;0.7&amp;#34; tokio = { version = …  ]]></content></entry><entry><title>19 | Unsafe Rust 与 FFI：踏入无人监管的领地</title><url>/blog/rust-19-unsafe-ffi/</url><categories><category>Rust</category></categories><tags><tag>Rust</tag><tag>unsafe</tag><tag>FFI</tag><tag>C interop</tag><tag>内存安全</tag></tags><content type="html"><![CDATA[  Rust 的招牌卖点是内存安全，但安全是有代价的——借用检查器的规则有时会拦住一些&amp;quot;你知道是对的，但编译器无法证明&amp;quot;的操作。unsafe 就是那把钥匙：打开一扇门，让你进入编译器不再保护的区域。
这篇文章聊清楚三件事：unsafe 边界在哪里、怎么安全地包装 unsafe、以及如何用 FFI 和 C 库对话。
unsafe 的本质：不是&amp;quot;危险代码&amp;quot;，是&amp;quot;你担保&amp;quot; 很多人看到 unsafe 关键字就紧张，以为写了 unsafe 代码就一定会崩。这个理解有偏差。
unsafe 的真实含义是：编译器将验证责任从自己转移给你。Safe Rust 里，编译器能证明内存安全；unsafe 里，编译器说&amp;quot;这段我证明不了，你来担保&amp;quot;。
你担保的是：这段代码在所有合法调用路径下不会引发未定义行为（Undefined Behavior，UB）。如果你担保错了，程序可能崩溃、数据可能损坏、安全漏洞可能出现——但这是你的责任，不是编译器的。
一个很重要的认知：unsafe 块只是告诉编译器&amp;quot;这里我负责&amp;quot;，它不会关闭 Rust 的所有检查。生命周期检查、类型检查、借用检查仍然在运行，只是解锁了五个额外操作。
五件只有 unsafe 才能做的事 Rust 精确划定了 unsafe 的边界：只有以下五类操作需要 unsafe，其他任何操作都是 safe 的。
1. 解引用裸指针 裸指针（raw pointer）是 *const T 和 *mut T，类似 C 的指针。创建裸指针是 safe 的，但解引用它需要 unsafe：
fn main() { let x = 42i32; let p: *const i32 = &amp;amp;x; // 创建裸指针，safe // 解引用裸指针，unsafe let val = unsafe { *p }; println!(&amp;#34;{}&amp;#34;, val); // 42 } 为什么解引用需要 unsafe？因为裸指针可能是空指针、悬空指针（指向已释放内存）、未对齐指针，编译器无法在编译期验证这些情况。
裸指针在哪里会出现？FFI 调用返回的指针、从整数转换的内存地址、手动内存布局。
2. 调用 unsafe 函数或方法 某些函数被标记为 unsafe …  ]]></content></entry><entry><title>18 | 宏系统精讲</title><url>/blog/rust-18-macros/</url><categories><category>Rust</category></categories><tags><tag>Rust</tag><tag>宏</tag><tag>macro</tag><tag>derive</tag><tag>serde</tag></tags><content type="html"><![CDATA[  前几篇我们一直在用宏，却没有正面解释它：println!、vec!、tokio::select!、#[derive(Debug)]……这些都是宏。这篇我们正面拆解宏系统——它是什么，怎么写，什么时候该用。
Rust 的宏和 C 的 #define 完全不同。C 的宏是文本替换，编译器看不见，调试地狱。Rust 的宏在语法树层面操作，编译器全程参与，类型安全，可调试。
为什么需要宏 先说宏解决的是什么问题。
有些代码模式用函数无法优雅表达：
可变参数：println! 接受任意数量的参数，普通函数做不到 语法扩展：vec![1, 2, 3] 创建 Vec，这是一种更自然的语法，函数调用做不到这个字面量感觉 代码生成：#[derive(Serialize)] 自动实现序列化，手写要几十行；宏帮你生成 本质上，宏是在编译期生成代码的工具。
声明宏：macro_rules! 声明宏是最常用的宏形式，用模式匹配的方式定义&amp;quot;输入长什么样，展开成什么代码&amp;quot;。
最简单的宏 macro_rules! say_hello { () =&amp;gt; { println!(&amp;#34;Hello, Rust!&amp;#34;); }; } fn main() { say_hello!(); // 展开为 println!(&amp;#34;Hello, Rust!&amp;#34;); } macro_rules! 后面是宏的名字，花括号里是一组规则，每条规则是 (模式) =&amp;gt; { 展开内容 }。
带参数的宏 macro_rules! greet { ($name:expr) =&amp;gt; { println!(&amp;#34;Hello, {}!&amp;#34;, $name); }; } fn main() { greet!(&amp;#34;World&amp;#34;); greet!(42); } $name:expr 是模式片段，$name 是捕获变量名，expr 是片段类型（fragment specifier），表示&amp;quot;匹配任意表达式&amp;quot;。
常用片段类型 片段类型 匹配内容 示例 expr 表达式 1 + 2、&amp;quot;hello&amp;quot;、foo() ident 标识符 变量名、函数名 ty 类型 i32、Vec&amp;lt;String&amp;gt; pat 模式 Some(x)、(a, b) …  ]]></content></entry><entry><title>17 | 并发模式实战</title><url>/blog/rust-17-concurrency-patterns/</url><categories><category>Rust</category></categories><tags><tag>Rust</tag><tag>并发</tag><tag>异步</tag><tag>Tokio</tag><tag>设计模式</tag></tags><content type="html"><![CDATA[  上一篇我们把 Tokio 的核心 API 过了一遍：spawn、select!、Channel、JoinSet、spawn_blocking。这篇进入真正的实战——用这些积木搭出生产级的并发模式。
这就像学会了锤子、螺丝刀、锯子之后，开始学怎么造家具。单独的工具你都懂了，但组合起来还需要经验和套路。
模式一：生产者 / 消费者 这是最经典的并发模式，几乎所有涉及异步处理的系统都在用：任务不断涌入（生产者），一批 worker 负责处理（消费者），中间用 Channel 解耦。
基础版：单消费者 use tokio::sync::mpsc; use tokio::time::{sleep, Duration}; #[derive(Debug)] struct Job { id: u32, payload: String, } async fn process_job(job: Job) -&amp;gt; String { sleep(Duration::from_millis(100)).await; format!(&amp;#34;processed: {} - {}&amp;#34;, job.id, job.payload) } #[tokio::main] async fn main() { let (tx, mut rx) = mpsc::channel::&amp;lt;Job&amp;gt;(100); // 生产者 let producer = tokio::spawn(async move { for i in 0..10 { tx.send(Job { id: i, payload: format!(&amp;#34;data_{}&amp;#34;, i) }).await.unwrap(); println!(&amp;#34;生产者: 提交 job {}&amp;#34;, i); } // tx 在这里 drop，消费者会收到 None 并自动退出 }); // 消费者 let consumer = tokio::spawn(async move { while let Some(job) = rx.recv().await { let result = process_job(job).await; println!(&amp;#34;消费者: {}&amp;#34;, result); } println!(&amp;#34; …  ]]></content></entry><entry><title>16 | Tokio 实战</title><url>/blog/rust-16-tokio-in-practice/</url><categories><category>Rust</category></categories><tags><tag>Rust</tag><tag>Tokio</tag><tag>异步</tag><tag>并发</tag><tag>select</tag><tag>spawn</tag></tags><content type="html"><![CDATA[  上一篇我们把异步的底层原理讲清楚了：Future 是状态机，poll 驱动执行，.await 是暂停点，Tokio 是运行时引擎。理论有了，这篇进入实战——Tokio 最常用的 API 怎么用，什么时候用，踩过哪些坑。
spawn：把任务丢给运行时 tokio::spawn 是最常用的 API，作用是把一个 async 任务提交给 Tokio 调度器，在后台运行。类比 Go 里的 go func()，或者线程池的 submit。
use tokio::time::{sleep, Duration}; #[tokio::main] async fn main() { // spawn 立刻返回一个 JoinHandle，任务在后台运行 let handle = tokio::spawn(async { sleep(Duration::from_millis(100)).await; println!(&amp;#34;任务完成&amp;#34;); 42 // 返回值 }); println!(&amp;#34;主流程继续执行，不等任务&amp;#34;); // 等待任务结束，拿到返回值 let result = handle.await.unwrap(); println!(&amp;#34;任务返回值: {}&amp;#34;, result); } JoinHandle 的两个用途：
等待任务完成（.await） 取消任务（.abort()） let handle = tokio::spawn(async { loop { sleep(Duration::from_secs(1)).await; println!(&amp;#34;还在跑...&amp;#34;); } }); sleep(Duration::from_secs(3)).await; handle.abort(); // 取消任务 println!(&amp;#34;任务已取消&amp;#34;); 所有权问题： spawn 要求 Future 是 &#39;static + Send。意思是闭包不能借用外部的非 &#39;static 引用，需要用 move 转移所有权，或者用 Arc 共享。
let data = String::from(&amp;#34;hello&amp;#34;); // 错误：data 是借用，不满足 &amp;#39;static // tokio::spawn(async { …  ]]></content></entry><entry><title>15 | Rust 异步编程基础</title><url>/blog/rust-15-async-basics/</url><categories><category>Rust</category></categories><tags><tag>Rust</tag><tag>异步</tag><tag>async</tag><tag>await</tag><tag>Future</tag><tag>Tokio</tag></tags><content type="html"><![CDATA[  上一篇我们聊了 Rust 的并发基础：线程、Mutex、Channel。那是&amp;quot;多线程&amp;quot;模型的并发——每个任务跑在独立线程上，操作系统负责调度。这套模型对 CPU 密集型任务很合适，但一旦遇到大量 I/O——网络请求、数据库查询、文件读写——就会遇到麻烦。
为什么需要 async 先看一个具体场景。假设你要同时处理 10000 个 HTTP 连接，每个连接大部分时间在等待客户端发数据。如果每个连接创建一个线程，就需要 10000 个线程。
问题在哪？线程不便宜。每个线程默认有几 MB 的栈空间，10000 个线程光内存就要几十 GB。更要命的是线程切换有开销——操作系统在线程间切换时要保存和恢复寄存器、切换内存上下文，这些开销在高并发时会显著拖慢系统。
Go 用 goroutine 解决这个问题：goroutine 是用户态轻量级线程，几十 KB 起步，运行时负责调度，可以轻松开百万个。Python 的 asyncio 也是类似思路——用协程避免线程开销。
Rust 的 async/await 走的是同一条路，但实现方式更底层、更零成本。理解它的关键是 Future Trait。
Future Trait：异步的核心抽象 Rust 标准库中 Future 的定义非常简单：
pub trait Future { type Output; fn poll(self: Pin&amp;lt;&amp;amp;mut Self&amp;gt;, cx: &amp;amp;mut Context&amp;lt;&amp;#39;_&amp;gt;) -&amp;gt; Poll&amp;lt;Self::Output&amp;gt;; } pub enum Poll&amp;lt;T&amp;gt; { Ready(T), Pending, } 这就是 Rust 异步的本质：一个可以被&amp;quot;轮询&amp;quot;的对象。每次调用 poll，它要么返回 Ready(值)（任务完成了），要么返回 Pending（还没好，待会再来）。
用餐厅取餐类比：你点了餐，服务员给你一个取餐号（Future）。你可以去问&amp;quot;好了吗？&amp;quot;（poll）。如果好了，拿走；没好，回去等。与其一直站在窗口等（阻塞），不如去做别的事，等被叫到再来（异步）。
Context 里包含一个 Waker——当 Future 准备好时，它会调用 waker.wake() 通知 …  ]]></content></entry><entry><title>14 | Rust 并发基础</title><url>/blog/rust-14-concurrency/</url><categories><category>Rust</category></categories><tags><tag>Rust</tag><tag>并发</tag><tag>线程</tag><tag>Mutex</tag><tag>Channel</tag><tag>Send</tag><tag>Sync</tag></tags><content type="html"><![CDATA[  Go 有一句名言：&amp;ldquo;不要通过共享内存来通信，而要通过通信来共享内存。&amp;ldquo;这句话出现的原因，是因为共享内存并发太容易出错——数据竞争、死锁、忘记加锁，这些 bug 在 C/C++ 里困扰了程序员几十年。
Rust 的答案更激进：在编译期就消灭数据竞争。不是靠运行时检测，不是靠程序员自律，而是让编译器拒绝编译任何可能产生数据竞争的代码。
这一切的底气来自所有权系统——那两个神秘的 Trait：Send 和 Sync。
Send 和 Sync：并发安全的编译期保证 Send 和 Sync 是 Rust 标准库里最简单也最重要的两个 Trait，它们没有任何方法，纯粹是标记：
// 标记 Trait，没有任何方法 pub unsafe auto trait Send {} pub unsafe auto trait Sync {} Send：类型的所有权可以在线程间转移。如果 T: Send，就可以把 T 的值从一个线程移到另一个线程。
Sync：类型可以被多个线程同时引用。如果 T: Sync，就可以在多个线程里同时持有 &amp;amp;T（等价于 &amp;amp;T: Send）。
绝大多数类型都自动实现了这两个 Trait——i32、String、Vec&amp;lt;T&amp;gt;（当 T: Send）等等。编译器会自动推导：只要类型的所有字段都是 Send，这个类型就是 Send；字段都是 Sync，这个类型就是 Sync。
哪些类型不是 Send/Sync？
use std::rc::Rc; use std::cell::RefCell; // Rc&amp;lt;T&amp;gt;：不是 Send，也不是 Sync // 原因：Rc 的引用计数用的是普通整数，不是原子操作，多线程访问会出问题 let rc = Rc::new(42); // thread::spawn(move || println!(&amp;#34;{}&amp;#34;, rc)); // 编译错误！Rc 不是 Send // RefCell&amp;lt;T&amp;gt;：是 Send（如果 T: Send），但不是 Sync // 原因：RefCell 的运行时借用检查不是线程安全的 let cell = RefCell::new(42); // 可以 move 到另一个线程，但不能同时被多个线程引用 对应的线程安全版本：Rc → …  ]]></content></entry><entry><title>13 | 闭包深入</title><url>/blog/rust-13-closures/</url><categories><category>Rust</category></categories><tags><tag>Rust</tag><tag>闭包</tag><tag>Fn</tag><tag>FnMut</tag><tag>FnOnce</tag></tags><content type="html"><![CDATA[  大多数语言都有 lambda 或匿名函数，用起来差不多：传个函数进去，里面能访问外部变量，搞定。
Rust 的闭包表面上也是这样，但背后的机制要复杂得多。Rust 需要在编译期确定闭包捕获了什么、以什么方式捕获，以及这个闭包能被调用几次——因为这直接关系到所有权和内存安全。
理解闭包，核心是理解三件事：捕获方式、三个 Fn Trait、move 关键字。
闭包长什么样 先认识一下 Rust 闭包的语法：
fn main() { let x = 5; // 闭包：用 |参数| 表达式 的形式 let add_x = |n| n + x; // 捕获了外部变量 x println!(&amp;#34;{}&amp;#34;, add_x(10)); // 输出 15 // 多参数，带类型标注（通常可以省略，编译器能推断） let multiply = |a: i32, b: i32| -&amp;gt; i32 { a * b }; println!(&amp;#34;{}&amp;#34;, multiply(3, 4)); // 输出 12 // 多行闭包用花括号 let process = |s: &amp;amp;str| { let trimmed = s.trim(); trimmed.to_uppercase() }; println!(&amp;#34;{}&amp;#34;, process(&amp;#34; hello &amp;#34;)); // 输出 HELLO } 和普通函数的区别：
参数用 || 包裹，不需要写 fn 关键字 可以省略参数类型和返回类型（大多数情况编译器能推断） 能捕获外部环境中的变量——这是普通函数做不到的 捕获方式：闭包怎么拿外部变量 Rust 的闭包可以用三种方式捕获外部变量，对应所有权系统的三种语义：
不可变借用（&amp;amp;T） 最轻量的捕获，只读访问：
fn main() { let message = String::from(&amp;#34;hello&amp;#34;); // 闭包只需要读 message，借用即可 let print_message = || println!(&amp;#34;{}&amp;#34;, message); print_message(); // 输出 hello print_message(); // 可以多次调用 // message 还能继续使用， …  ]]></content></entry><entry><title>12 | 错误处理体系</title><url>/blog/rust-12-error-handling/</url><categories><category>Rust</category></categories><tags><tag>Rust</tag><tag>错误处理</tag><tag>Result</tag><tag>thiserror</tag><tag>anyhow</tag></tags><content type="html"><![CDATA[  每门语言都有自己处理错误的哲学。Python 和 Java 用异常——出错时向上抛，谁想处理谁 catch，不想处理就继续往上冒。Go 用多返回值——每个函数都返回 (result, error)，调用方强制检查。
Rust 的答案更彻底：错误是类型，必须被处理，不能被忽视。编译器会在你忽略错误时发出警告，在你错误地传播时拒绝编译。
这带来了一个结果：Rust 程序很少有&amp;quot;运行时突然崩溃&amp;quot;的意外——因为大多数错误在编译期就被逼着处理掉了。
两类错误：panic 和 Result Rust 把错误分成两类，处理方式截然不同。
不可恢复错误：panic! panic! 代表&amp;quot;程序遇到了不应该发生的事&amp;quot;，立即终止当前线程，打印错误信息和调用栈。
fn main() { let v = vec![1, 2, 3]; println!(&amp;#34;{}&amp;#34;, v[10]); // panic: index out of bounds } 什么时候该用 panic？
测试代码：unwrap()、expect() 在测试里完全合理 原型和脚本：先让代码跑起来，后面再处理错误 违反不变式：调用方传入了不合法的参数（程序员的错误，不是用户的错误） 确实无法继续：比如启动时配置文件格式完全错误 什么时候不该用 panic？
库代码：永远不要在库里 panic，把错误返回给调用方 可预期的失败：网络超时、文件不存在、用户输入非法——这些都是正常情况，用 Result // 坏的写法——库函数里 panic fn parse_config(s: &amp;amp;str) -&amp;gt; Config { serde_json::from_str(s).unwrap() // 调用方无法处理这个 panic } // 好的写法——返回 Result fn parse_config(s: &amp;amp;str) -&amp;gt; Result&amp;lt;Config, serde_json::Error&amp;gt; { serde_json::from_str(s) } Result：把错误变成类型 Result&amp;lt;T, E&amp;gt; 是 Rust 错误处理的核心：
enum Result&amp;lt;T, E&amp;gt; { Ok(T), // 成功，携带结果值 Err(E), // …  ]]></content></entry><entry><title>11 | Iterator——最值得深入的 Trait</title><url>/blog/rust-11-iterator/</url><categories><category>Rust</category></categories><tags><tag>Rust</tag><tag>Iterator</tag><tag>迭代器</tag><tag>函数式编程</tag><tag>零成本抽象</tag></tags><content type="html"><![CDATA[  你可能写过这样的代码：遍历一个列表，过滤掉不符合条件的，对剩下的做个变换，最后求和。在 Python 里，这是一行 list comprehension；在 Go 里，这是三个嵌套的 for 循环。
Rust 的答案是迭代器——既不牺牲可读性，也不牺牲性能。这是 Rust 标准库里设计最精妙的 Trait 之一，值得专门深入。
迭代器的本质：一个会&amp;quot;吐出&amp;quot;值的东西 Iterator Trait 的定义出奇地简单：
pub trait Iterator { type Item; fn next(&amp;amp;mut self) -&amp;gt; Option&amp;lt;Self::Item&amp;gt;; // 其余几十个方法都有默认实现 } 只有一个必须实现的方法：next。调用一次，得到下一个值（Some）；没有更多值了，返回 None。就这么简单。
fn main() { let v = vec![1, 2, 3]; let mut iter = v.iter(); // 创建迭代器 println!(&amp;#34;{:?}&amp;#34;, iter.next()); // Some(1) println!(&amp;#34;{:?}&amp;#34;, iter.next()); // Some(2) println!(&amp;#34;{:?}&amp;#34;, iter.next()); // Some(3) println!(&amp;#34;{:?}&amp;#34;, iter.next()); // None } for 循环不过是 next 的语法糖——编译器把它展开成一个循环，反复调用 next 直到返回 None：
// 这两段代码完全等价 for x in vec![1, 2, 3] { println!(&amp;#34;{}&amp;#34;, x); } // 展开后的等价形式 let mut iter = vec![1, 2, 3].into_iter(); while let Some(x) = iter.next() { println!(&amp;#34;{}&amp;#34;, x); } 惰性求值：不求值，直到你需要 迭代器最重要的特性是惰性——在你真正消费它之前，什么都不会发生。
fn main() { let v = vec![1, 2, 3, 4, 5]; // 这里什么都没发生，只是描述了&amp;#34; …  ]]></content></entry><entry><title>10 | 泛型与常用标准库 Trait</title><url>/blog/rust-10-generics-stdlib-traits/</url><categories><category>Rust</category></categories><tags><tag>Rust</tag><tag>泛型</tag><tag>Trait</tag><tag>标准库</tag></tags><content type="html"><![CDATA[  想象你在写一个&amp;quot;找最大值&amp;quot;的函数。第一版针对 i32，写完了，测试通过。然后产品说还要支持 f64，你复制了一遍。接着又要支持字符串……你开始怀疑人生：明明逻辑完全一样，为什么要写三遍？
泛型就是解决这个问题的。而当泛型和上一篇的 Trait 结合，你能做的事就更多了——不只是&amp;quot;任意类型&amp;quot;，而是&amp;quot;满足某些能力的任意类型&amp;quot;。
本篇分两部分：先讲泛型的语法和性能本质，再系统梳理日常必须掌握的标准库 Trait。这些 Trait 是写地道 Rust 代码的基础词汇，绕不开。
泛型：写一次，适用多种类型 泛型的核心思想很朴素：与其为每种类型写一遍相同的逻辑，不如用一个类型占位符，让编译器替你展开。
泛型函数 // 不用泛型，要为每种类型写一遍 fn largest_i32(list: &amp;amp;[i32]) -&amp;gt; &amp;amp;i32 { let mut largest = &amp;amp;list[0]; for item in list { if item &amp;gt; largest { largest = item; } } largest } // 用泛型，一个函数搞定——但要加 Trait Bound fn largest&amp;lt;T: PartialOrd&amp;gt;(list: &amp;amp;[T]) -&amp;gt; &amp;amp;T { let mut largest = &amp;amp;list[0]; for item in list { if item &amp;gt; largest { largest = item; } } largest } T: PartialOrd 是 Trait Bound，告诉编译器：传入的 T 必须支持比较操作（&amp;gt;）。没有这个约束，编译器不知道 item &amp;gt; largest 是否合法。
泛型结构体 struct Point&amp;lt;T&amp;gt; { x: T, y: T, } impl&amp;lt;T&amp;gt; Point&amp;lt;T&amp;gt; { fn x(&amp;amp;self) -&amp;gt; &amp;amp;T { &amp;amp;self.x } } // 特化：只为 f64 实现某些方法 impl Point&amp;lt;f64&amp;gt; { fn distance_from_origin(&amp;amp;self) …  ]]></content></entry><entry><title>09 | Trait——Rust 的接口</title><url>/blog/rust-09-traits/</url><categories><category>Rust</category></categories><tags><tag>Rust</tag><tag>Trait</tag><tag>接口</tag><tag>动态分发</tag><tag>泛型</tag></tags><content type="html"><![CDATA[  前一篇讲智能指针，我们见识了 Box&amp;lt;T&amp;gt;、Rc&amp;lt;T&amp;gt;、Arc&amp;lt;T&amp;gt; 等工具，它们的核心作用是管理数据的所有权与生命周期。但如果问&amp;quot;这些类型的行为如何定义&amp;quot;，答案指向的是本篇主角——Trait。
Trait 是 Rust 的接口机制，也是整个类型系统的黏合剂。理解 Trait，是从&amp;quot;能写 Rust&amp;quot;到&amp;quot;写好 Rust&amp;quot;的关键一步。
从一个问题开始 假设你要写一个函数，给不同的图形计算面积：圆形、矩形、三角形。在 Go 里，你会定义一个 interface；在 Python 里，你会用 duck typing；在 Rust 里，你需要 Trait。
先看 Trait 的基本语法，再和 Go/Python 做对比，最后深入几个关键细节。
定义 Trait Trait 的定义很像其他语言的接口，用 trait 关键字：
trait Area { fn area(&amp;amp;self) -&amp;gt; f64; } 这定义了一个 Area Trait，任何实现了它的类型都必须提供 area 方法。
接下来为具体类型实现 Trait：
struct Circle { radius: f64, } struct Rectangle { width: f64, height: f64, } impl Area for Circle { fn area(&amp;amp;self) -&amp;gt; f64 { std::f64::consts::PI * self.radius * self.radius } } impl Area for Rectangle { fn area(&amp;amp;self) -&amp;gt; f64 { self.width * self.height } } 语法是 impl TraitName for TypeName，清晰明了。
默认方法 Trait 可以提供默认实现，不强制每个类型都覆盖：
trait Describe { fn name(&amp;amp;self) -&amp;gt; String; fn describe(&amp;amp;self) -&amp;gt; String { format!(&amp;#34;我是 {}&amp;#34;, self.name()) } } struct Dog; …  ]]></content></entry><entry><title>08 | 智能指针——当引用不够用时</title><url>/blog/rust-08-smart-pointers/</url><categories><category>Rust</category></categories><tags><tag>Rust</tag><tag>智能指针</tag><tag>Box</tag><tag>Rc</tag><tag>Arc</tag><tag>RefCell</tag></tags><content type="html"><![CDATA[  前两篇，我们深入理解了借用和生命周期。Rust 的引用机制很强大，但有一个根本限制：引用不拥有数据。
当你需要在多个地方共享同一份数据的所有权，或者需要在堆上存储数据而不想手动管理内存时，普通引用就不够用了。这时候，智能指针登场。
什么是智能指针 先说普通指针。在 C/C++ 里，指针就是一个数字，存着内存地址，没有任何附加功能，忘记释放就内存泄漏，多次释放就崩溃。
智能指针在指针的基础上，附加了&amp;quot;智能&amp;quot;——通过实现 Drop trait 在离开作用域时自动清理，通过实现 Deref trait 让你像使用引用一样使用它。
在 Rust 中，String 和 Vec&amp;lt;T&amp;gt; 本质上都是智能指针。本篇重点讲四个核心类型：Box&amp;lt;T&amp;gt;、Rc&amp;lt;T&amp;gt;、Arc&amp;lt;T&amp;gt;、RefCell&amp;lt;T&amp;gt;。
Box&amp;lt;T&amp;gt;：最简单的堆分配 基本用法 fn main() { let b = Box::new(5); println!(&amp;#34;b = {}&amp;#34;, b); // 像普通变量一样使用 } // b 离开作用域，堆内存自动释放 Box&amp;lt;T&amp;gt; 把数据放在堆上，自身（一个指针）存在栈上。当 Box 离开作用域，堆上的数据也被释放。
使用场景一：递归类型 Rust 在编译期需要知道每个类型的大小。递归类型（自引用）会导致大小无法确定：
// 编译错误：recursive type has infinite size enum List { Cons(i32, List), Nil, } 用 Box 包一层，把&amp;quot;未知大小&amp;quot;变成&amp;quot;指针大小&amp;quot;（固定 8 字节）：
enum List { Cons(i32, Box&amp;lt;List&amp;gt;), Nil, } fn main() { let list = List::Cons(1, Box::new(List::Cons(2, Box::new(List::Cons(3, Box::new(List::Nil)))))); } 使用场景二：Trait 对象 当你需要存储实现了某个 trait 的不同类型时：
trait Animal { fn speak(&amp;amp;self); } struct Dog; …  ]]></content></entry><entry><title>07 | 生命周期——引用的有效期限</title><url>/blog/rust-07-lifetime/</url><categories><category>Rust</category></categories><tags><tag>Rust</tag><tag>生命周期</tag><tag>引用</tag><tag>借用检查器</tag></tags><content type="html"><![CDATA[  上一篇我们学了借用与引用，知道 &amp;amp;T 可以让你&amp;quot;看&amp;quot;数据而不转移所有权。但有个问题留着没讲：
引用到底能用多久？
如果数据已经被释放，但引用还指向那块地址，会发生什么？Rust 是怎么在编译期发现这个问题的？
这就是**生命周期（Lifetime）**要解决的事。
生命周期是 Rust 里最让初学者头疼的概念，主要原因是语法看起来很陌生：&#39;a、&#39;b、&#39;static……像某种暗语。但它的核心思想其实很朴素，类比一下就清楚了。
租约类比：引用就是一份有期限的借据 你向朋友借一本书，写了一张借据：&amp;ldquo;我借了这本书，2025年底还。&amp;rdquo;
这张借据有一个有效期。如果你的朋友在2025年底之前把书卖掉了，你的借据就失效了——书不存在了，借据还指向那本书，是没有意义的。
Rust 的引用和生命周期，就是这套逻辑：
数据（书）有它自己的生命周期——它在内存里存活的时间段 引用（借据）也有一个有效期 规则：引用的有效期不能超过它所指向的数据的生命周期 编译器的借用检查器，就是在帮你检查&amp;quot;借据是否会比书活得更久&amp;quot;。
悬空引用：借据还在，书已消失 最直接的例子，C/C++ 里的经典 bug：
// C 代码（危险！） int* get_number() { int x = 42; // x 在栈上 return &amp;amp;x; // 返回 x 的地址 } // 函数返回，x 被销毁——地址指向垃圾数据 int main() { int* p = get_number(); printf(&amp;#34;%d\n&amp;#34;, *p); // 未定义行为，可能崩溃 } 用借据类比：书已经烧掉了，你还拿着借据去找书，找到的是灰烬。
Rust 在编译期就拒绝了这种写法：
fn get_number() -&amp;gt; &amp;amp;i32 { // 编译错误！ let x = 42; &amp;amp;x // 想返回 x 的引用 } // x 在这里被 drop，引用将指向无效内存 编译器报错：returns a reference to data owned by the current function
修复很简单——把值本身返回出去，而不是引用：
fn get_number() -&amp;gt; i32 { // 返回值，转移所有权 let x = …  ]]></content></entry><entry><title>06 | 借用与引用——不转移所有权，也能用你的数据</title><url>/blog/rust-06-borrowing/</url><categories><category>Rust</category></categories><tags><tag>Rust</tag><tag>借用</tag><tag>引用</tag><tag>借用检查器</tag></tags><content type="html"><![CDATA[  上一篇讲到一个尴尬的问题：把值传给函数，所有权就转移了，函数用完你就再也拿不回来了。
fn print_length(s: String) { println!(&amp;#34;长度是：{}&amp;#34;, s.len()); } // s 在这里被 drop fn main() { let name = String::from(&amp;#34;Alice&amp;#34;); print_length(name); // println!(&amp;#34;{}&amp;#34;, name); // 编译错误！name 已经没了 } 为了&amp;quot;打印一下长度&amp;quot;就把数据永久交出去，这显然不合理。实际写代码时，大多数情况我们只是想让函数&amp;quot;看一眼&amp;quot;数据，并不想转移所有权。
Rust 为此提供了借用（Borrowing）机制——通过引用（Reference），让别人使用你的数据，但所有权还在你这里。
图书馆借书：引用的直觉模型 想象你有一本书，朋友想看。
有两种方式：
把书送给朋友（Move）——书就不是你的了 借给朋友看（引用）——你还是书的主人，朋友看完还你 引用就是第二种方式。你把数据&amp;quot;借&amp;quot;给别人用，借的人用完了，数据还在你这里。
Rust 里，引用用 &amp;amp; 符号表示：
fn print_length(s: &amp;amp;String) { // 接收引用，不拿走所有权 println!(&amp;#34;长度是：{}&amp;#34;, s.len()); } fn main() { let name = String::from(&amp;#34;Alice&amp;#34;); print_length(&amp;amp;name); // 传引用，&amp;amp;name 就是&amp;#34;把 name 借出去&amp;#34; println!(&amp;#34;name 还在：{}&amp;#34;, name); // 没问题，所有权从没离开过 } &amp;amp;name 的意思是：创建一个指向 name 的引用，而不是转移 name 本身。
引用在内存里长什么样 name 变量（栈）： ┌─────────────────────────┐ │ ptr ───────────────────┼──→ 堆：&amp;#34;Alice&amp;#34; │ len: 5 │ │ cap: 5 │ …  ]]></content></entry><entry><title>05 | 所有权——Rust 最重要的概念</title><url>/blog/rust-05-ownership/</url><categories><category>Rust</category></categories><tags><tag>Rust</tag><tag>所有权</tag><tag>内存管理</tag><tag>Move语义</tag></tags><content type="html"><![CDATA[  如果你用 Go 或 Python 写过代码，大概从来没想过一个问题：这段数据，到底谁负责把它从内存里删掉？
在 Go 里，你 new 一个对象，用完了不管，GC 会帮你清。在 Python 里更是如此，变量一多，引用计数器自动追踪，没人用了就回收。这套机制很舒服，但代价是你对内存的生命周期几乎没有控制权，而且 GC 随时可能暂停程序做垃圾回收（Stop-The-World），高性能场景下是个大麻烦。
Rust 的答案是：既不让你手动 free，也不搞 GC，而是在编译期就把内存的生命周期推断清楚。这套机制叫做所有权（Ownership）。
理解所有权，是从&amp;quot;会写 Go/Python&amp;quot;到&amp;quot;会写 Rust&amp;quot;的关键一跳。
一套房子只能有一个产权人 在讲代码之前，先建立一个直觉。
假设你有一套房子，这套房子有一本产权证。产权证上只能登记一个人的名字——这个人就是这套房子的所有者（Owner）。
产权人可以住在这套房子里（使用这块内存） 产权人可以把房子转让给别人（Move，所有权转移） 产权人搬走、消失时，这套房子就会被收回（内存释放） 同一时刻，不能有两个人都拿着这套房子的产权证——否则都消失时，房子会被收回两次（double-free，程序崩溃） 这就是 Rust 所有权的核心逻辑。
从内存问题出发：GC 解决了什么，又带来了什么 写 Go 时你可能遇到过这种情况：
// Go 代码 func newUser() *User { u := &amp;amp;User{Name: &amp;#34;Alice&amp;#34;} return u // 返回局部变量的指针，Go 的 GC 会保证它活着 } 这在 Go 里完全没问题。GC 在后台追踪所有对象，只要还有人引用它，就不释放。
但 GC 有两个代价：
Stop-The-World（STW）停顿：GC 运行时需要暂停程序，哪怕只是几毫秒，对实时性要求高的系统（游戏引擎、交易系统）也不能接受。 无法精确控制释放时机：你无法保证一个资源（比如数据库连接、文件句柄）在某个精确的时刻被释放，只能用 defer 或 context 来模拟。 C/C++ 让程序员手动 malloc/free，没有 GC 停顿，但极容易出错：
use-after-free：内存释放后继续用， …  ]]></content></entry><entry><title>04 | 高频语法精讲（下）——集合、字符串与错误速通</title><url>/blog/rust-04-syntax-basics-2/</url><categories><category>Rust</category></categories><tags><tag>Rust</tag><tag>集合</tag><tag>字符串</tag><tag>错误处理</tag></tags><content type="html"><![CDATA[  上一篇把变量、类型和控制流讲完了。这一篇继续扫尾——把日常开发里用得最多的五个&amp;quot;基础设施&amp;quot;过一遍：两个集合类型、字符串的基本认知、以及错误处理的入门写法。
这些内容本身不复杂，但每一个都有值得单独说清楚的地方。
Vec：Rust 的动态数组 Vec&amp;lt;T&amp;gt; 是 Rust 里最常用的集合，对应 Go 的 slice、Python 的 list。
fn main() { // 创建 let mut v: Vec&amp;lt;i32&amp;gt; = Vec::new(); let v2 = vec![1, 2, 3]; // 宏语法，更常用 // 添加元素 v.push(10); v.push(20); v.push(30); // 访问：两种方式 let first = v[0]; // 越界会 panic let second = v.get(1); // 返回 Option&amp;lt;&amp;amp;i32&amp;gt;，更安全 // 遍历 for item in &amp;amp;v { println!(&amp;#34;{}&amp;#34;, item); } // 遍历并修改 for item in &amp;amp;mut v { *item *= 2; } println!(&amp;#34;{:?}&amp;#34;, v); // [20, 40, 60] } 几个常用操作：
let mut v = vec![3, 1, 4, 1, 5, 9, 2, 6]; v.sort(); // 排序 v.dedup(); // 去重（需要先排序） v.retain(|&amp;amp;x| x &amp;gt; 3); // 保留满足条件的元素 let sum: i32 = v.iter().sum(); // 求和 let len = v.len(); // 长度 v.pop(); // 弹出最后一个 v.insert(0, 99); // 在指定位置插入 和 Go slice 最大的区别：Vec 有明确的所有权，把 Vec 传给函数会发生 move，除非你传引用。这一点到所有权章节会详细讲，现在先有个印象。
HashMap：键值存储 use std::collections::HashMap; fn main() { let mut scores: HashMap&amp;lt;String, i32&amp;gt; = …  ]]></content></entry><entry><title>03 | 高频语法精讲（上）——变量、类型与控制流</title><url>/blog/rust-03-syntax-basics-1/</url><categories><category>Rust</category></categories><tags><tag>Rust</tag><tag>变量</tag><tag>类型系统</tag><tag>模式匹配</tag></tags><content type="html"><![CDATA[  有了前两篇的认知框架和工具链基础，现在正式进入 Rust 的语法世界。
这一篇不打算把所有语法过一遍——那样做没有意义，查文档比看文章快得多。这里重点讲那些用得最频繁、最容易和 Go/Python 产生认知冲突的部分。
变量绑定：默认不可变 Rust 里声明变量用 let，但和 Go/Python 最大的不同是：变量默认是不可变的（immutable）。
fn main() { let x = 5; x = 6; // 编译错误：cannot assign twice to immutable variable } 要让变量可变，需要显式加 mut：
fn main() { let mut x = 5; x = 6; println!(&amp;#34;{}&amp;#34;, x); // 6 } 这个设计是刻意的。Rust 鼓励你默认写不可变变量，需要改变时再加 mut——这样一眼就能看出哪些变量会被修改，代码意图更清晰，也减少了意外修改的风险。
和 Go 对比：Go 里 var x int = 5 默认可变，想不可变要用常量 const。Rust 反过来，默认不可变，想可变加 mut。
和 Python 对比：Python 里变量随时可以重新绑定，没有&amp;quot;不可变变量&amp;quot;的概念。Rust 的不可变是绑定层面的——这个名字绑定的值不能变。
Shadowing：同名重新绑定 Rust 允许用同名的 let 遮蔽（shadow）之前的变量：
fn main() { let x = 5; let x = x + 1; // shadowing：新的 x 遮蔽了旧的 x let x = x * 2; println!(&amp;#34;{}&amp;#34;, x); // 12 } shadowing 和 mut 的关键区别是：shadowing 允许改变类型，而 mut 不允许。
fn main() { let spaces = &amp;#34; &amp;#34;; // &amp;amp;str 类型 let spaces = spaces.len(); // usize 类型，类型变了！ println!(&amp;#34;{}&amp;#34;, spaces); // 3 } 如果用 mut 尝试改变类型：
let mut spaces = &amp;#34; &amp;#34;; spaces = spaces.len(); …  ]]></content></entry><entry><title>02 | 工具链与项目结构：Rust 的工程基础</title><url>/blog/rust-02-toolchain/</url><categories><category>Rust</category></categories><tags><tag>Rust</tag><tag>Cargo</tag><tag>工具链</tag></tags><content type="html"> 在正式写 Rust 代码之前，先把工具链搞清楚。工具用顺手了，后面的学习会省很多力气。
Rust 的工具链设计得相当完整，安装好之后你会得到一整套开箱即用的工具：编译器、包管理器、格式化工具、代码检查工具——它们都来自官方，不需要到处找第三方替代品。
rustup：管理 Rust 本身 rustup 是 Rust 的工具链管理器，类似 Python 的 pyenv 或 Node 的 nvm。它负责安装、更新和切换不同版本的 Rust。
安装 Rust：
curl --proto &amp;amp;#39;=https&amp;amp;#39; --tlsv1.2 -sSf https://sh.rustup.rs | sh 安装完成后，rustup 会在你的 PATH 里加入 ~/.cargo/bin，所有工具都放在这里。
常用命令：
# 更新到最新稳定版 rustup update # 查看当前版本 rustc --version cargo --version # 安装 nightly 版本（某些实验性特性需要） rustup install nightly # 为当前项目切换到 nightly rustup override set nightly # 查看已安装的工具链 rustup toolchain list 三个发布频道：
stable：稳定版，每六周发布一次，生产环境用这个 beta：下一个 stable 的预览版 nightly：每日构建，包含最新的实验性特性，某些库（比如早期的 async trait）依赖它 绝大多数情况下用 stable 就够了。
cargo：Rust 的核心工具 cargo 是 Rust 的构建系统和包管理器，它把编译、测试、依赖管理、发布全包了。和 Go 的 go 命令类似，但功能更丰富。
创建项目：
# 创建二进制项目（可执行程序） cargo new my-app # 创建库项目 cargo new my-lib --lib # 在已有目录里初始化 cargo init 新建项目后的目录结构：
my-app/ ├── Cargo.toml # 项目配置文件（类似 package.json / go.mod） ├── Cargo.lock # 依赖锁定文件（自动生成） └── src/ └── main.rs # 程序入口 常用命令：
# 编 …</content></entry><entry><title>01 | 为什么是 Rust？它到底解决了什么问题</title><url>/blog/rust-01-why-rust/</url><categories><category>Rust</category></categories><tags><tag>Rust</tag><tag>系统编程</tag><tag>内存安全</tag></tags><content type="html"> 在学一门语言之前，先搞明白它为什么存在，比死记语法有用得多。
Rust 不是凭空冒出来的。它的诞生，是软件行业几十年来一个没有被彻底解决的矛盾逼出来的。
那个悬而未决的矛盾 写程序，从来都有两个方向可以走。
第一条路：追求极致性能，自己管内存。
C 和 C++ 走的就是这条路。你需要什么内存，自己 malloc，用完自己 free。操作系统把控制权完全交给你，没有任何额外开销，代码跑得飞快。Linux 内核、数据库引擎、浏览器的渲染器——这类软件都选这条路。
但代价是什么？内存管理是出了名的容易出错：
忘了 free，内存泄漏，程序越跑越慢，最终 OOM free 了之后还在用，悬空指针（use-after-free），直接崩溃，或者更糟，被攻击者利用 用了数组越界，栈溢出，缓冲区溢出——这类漏洞至今仍是安全漏洞的主要来源 微软曾统计过，其安全补丁里大约 70% 是内存安全相关的 bug。Google 的 Chrome 团队给出了类似的数字。这些 bug 不是因为工程师水平差，而是因为手动内存管理本身就是一件很难做对的事。
第二条路：让运行时帮你管内存。
Java、Go、Python 走的是这条路。有垃圾回收器（GC），你分配内存，GC 自动回收，不需要操心悬空指针，不会内存泄漏（通常）。
代价也是明摆着的：
GC 有暂停（Stop-the-World），哪怕是 Go 这种已经优化得很好的并发 GC，在某些场景下仍然会有延迟抖动 运行时本身有开销，内存占用更高 无法精细控制内存布局，某些底层需求做不到 所以这两条路，长期以来是一个二选一的问题：要性能，就手动管内存冒风险；要安全，就用 GC 付出代价。
Rust 的答案 Rust 说：这两条路不是非此即彼，有第三种选择。
Rust 的核心思路是：把内存管理的正确性放到编译期来保证。 不靠 GC，不靠程序员的自律，而是让编译器通过一套严格的规则，在你的代码进入运行时之前就拒绝掉所有可能的内存错误。
这套规则的核心就是所有权系统（Ownership），我们后面会专门用几篇文章来讲它。但在这里，你只需要知道一个结论：
Rust 的编译器能够在不引入 GC 的前提下，从静态分析层面保证内存安全。
这意味着：
没有悬空指针——编译器不允许你持有一个已经被释放的值的引用 没有数据竞争——编译器不允许多个线程同时读写同一块内存（在 …</content></entry><entry><title>番外 | 与 APISIX 对比：手写还是用成品？</title><url>/blog/openresty-bonus-apisix/</url><categories><category>OpenResty</category></categories><tags><tag>OpenResty</tag><tag>APISIX</tag><tag>网关</tag><tag>选型</tag></tags><content type="html"> 走完这 30 篇，你已经能手写一个具备生产能力的 API 网关。但现实中，很多团队会选择直接用 APISIX，而不是从零开始写。这篇来聊清楚这两条路的区别，帮你做出合适的选择。
APISIX 是什么 APISIX 是 Apache 软件基金会的顶级项目，由 API7.ai 主导开发。它的底层正是 OpenResty——Nginx + LuaJIT。
简单说：APISIX 是一个用 OpenResty 写好的、功能完整的 API 网关产品。你在这个系列里手写的路由、鉴权、限流、熔断、插件系统……APISIX 都已经实现好了，而且经过了大规模生产验证。
APISIX 的技术特点：
etcd 存储路由：路由、插件配置存在 etcd，实时推送到每个实例，毫秒级生效 插件丰富：80+ 内置插件，覆盖鉴权、限流、可观测性、流量管理各类场景 Dashboard：提供 Web 管理界面，可以可视化配置路由和插件 Admin API：RESTful 接口管理一切，方便自动化集成 高性能：单核可处理数万 RPS，延迟极低 功能对比 功能 手写网关 APISIX 路由匹配 自己实现 内置，支持前缀/精确/正则/域名 JWT 鉴权 自己实现 jwt-auth 插件，开箱即用 API Key 自己实现 key-auth 插件 限流 自己实现 limit-req/limit-count/limit-conn 三种 熔断 自己实现 api-breaker 插件 请求改写 自己实现 proxy-rewrite/response-rewrite 插件 灰度发布 自己实现 traffic-split 插件 日志 自己实现 http-logger/kafka-logger/clickhouse-logger 等 监控 自己实现 prometheus 插件，内置 Grafana Dashboard 配置热更新 Redis + 定时器 etcd 推送，毫秒级 管理界面 无 Dashboard + Admin API 负载均衡 自己实现 轮询/加权/一致性哈希/EWMA 服务发现 无 支持 Nacos/Consul/Eureka/K8s 可以看到，APISIX 在功能完整性上全面领先手写方案。那手写还有意义吗？
手写网关的适用场景 深度定制的业务逻辑
当你的网关需要做非常特殊的事情——比如私有的签名协议、特殊 …</content></entry><entry><title>31 | 部署与运维</title><url>/blog/openresty-31-deployment/</url><categories><category>OpenResty</category></categories><tags><tag>OpenResty</tag><tag>部署</tag><tag>Docker</tag><tag>监控</tag><tag>运维</tag></tags><content type="html"><![CDATA[  代码写完、测试通过、调优完毕，最后一步是部署到生产环境。这篇来讲容器化部署、优雅重启和监控接入，以及一份生产环境检查清单。
Docker 容器化部署 Dockerfile FROM openresty/openresty:1.25.3-alpine # 安装 OPM（OpenResty 包管理器） RUN opm get ledgetech/lua-resty-http \ &amp;amp;&amp;amp; opm get SkyLothar/lua-resty-jwt \ &amp;amp;&amp;amp; opm get doujiang24/lua-resty-kafka # 复制配置和代码 COPY conf/nginx.conf /usr/local/openresty/nginx/conf/nginx.conf COPY lua/ /usr/local/openresty/nginx/lua/ # 创建日志目录 RUN mkdir -p /var/log/nginx # 健康检查 HEALTHCHECK --interval=10s --timeout=3s --start-period=5s --retries=3 \ CMD curl -sf http://localhost:8080/health || exit 1 EXPOSE 8080 CMD [&amp;#34;/usr/local/openresty/nginx/sbin/nginx&amp;#34;, &amp;#34;-g&amp;#34;, &amp;#34;daemon off;&amp;#34;] 健康检查接口 location /health { access_log off; content_by_lua_block { -- 检查关键依赖 local redis = require &amp;#34;resty.redis&amp;#34; local red = redis:new() red:set_timeout(500) local ok = red:connect(&amp;#34;redis&amp;#34;, 6379) if not ok then ngx.status = 503 ngx.say(&amp;#39;{&amp;#34;status&amp;#34;:&amp;#34;unhealthy&amp;#34;,&amp;#34;redis&amp;#34;:&amp;#34;down&amp;#34;}&amp;#39;) …  ]]></content></entry><entry><title>30 | 性能调优</title><url>/blog/openresty-30-performance-tuning/</url><categories><category>OpenResty</category></categories><tags><tag>OpenResty</tag><tag>性能</tag><tag>压测</tag><tag>调优</tag></tags><content type="html"> 系统上线前，最后一件事是调优和压测。不调优直接上线，性能可能只发挥了三四成；不压测就上线，遇到流量高峰会猝不及防。这篇把 OpenResty 性能调优的关键参数和压测方法系统整理一遍。
worker 数量与 CPU 绑定 worker 数量 # 自动设置为 CPU 核心数（推荐） worker_processes auto; # 或者手动指定 worker_processes 8; auto 会读取 CPU 核心数并自动设置。对于 I/O 密集型场景（OpenResty 通常是），worker 数量等于 CPU 核心数是最优的——更多的 worker 会增加上下文切换开销，更少则浪费 CPU。
CPU 亲和性绑定 把每个 worker 绑定到固定的 CPU 核心，减少跨核调度，提升 CPU 缓存命中率：
worker_processes 4; worker_cpu_affinity 0001 0010 0100 1000; # 4 个 worker 分别绑定到 CPU 0、1、2、3 # 自动绑定（Nginx 1.9.10+） worker_cpu_affinity auto; worker 连接数 events { worker_connections 10240; # 每个 worker 最大连接数 use epoll; # Linux 下用 epoll（默认已选择） multi_accept on; # 一次接受多个新连接 } 系统最大文件描述符数要配合调整：
# /etc/security/limits.conf nginx soft nofile 65535 nginx hard nofile 65535 # 或者在 nginx.conf 里 worker_rlimit_nofile 65535; 连接池参数调优 连接池的参数影响深远，设置不合理会导致连接频繁建立销毁，或者资源浪费。
Redis 连接池 -- 连接池计算公式： -- pool_size = worker_connections * 平均每请求Redis操作数 * 平均Redis操作耗时 / 平均请求耗时 -- 简化经验值：50～200，根据压测结果调整 red:set_keepalive( 10000, -- max_idle_timeout：连接在池中最长空闲时间（ms） 100 -- …</content></entry><entry><title>29 | 测试与调试</title><url>/blog/openresty-29-testing/</url><categories><category>OpenResty</category></categories><tags><tag>OpenResty</tag><tag>测试</tag><tag>调试</tag></tags><content type="html"><![CDATA[  写完代码，怎么验证它是对的？线上出了问题，怎么快速定位？这篇来讲 OpenResty 项目的测试和调试方法。
busted 单元测试 busted 是 Lua 生态里最成熟的测试框架，语法类似 RSpec/Jest。
安装 luarocks install busted 基本用法 -- spec/utils_spec.lua local describe = describe local it = it local assert = require &amp;#34;luassert&amp;#34; local utils = require &amp;#34;lib.utils&amp;#34; describe(&amp;#34;utils.slugify&amp;#34;, function() it(&amp;#34;converts spaces to hyphens&amp;#34;, function() assert.equals(&amp;#34;hello-world&amp;#34;, utils.slugify(&amp;#34;hello world&amp;#34;)) end) it(&amp;#34;lowercases the string&amp;#34;, function() assert.equals(&amp;#34;hello&amp;#34;, utils.slugify(&amp;#34;HELLO&amp;#34;)) end) it(&amp;#34;removes special characters&amp;#34;, function() assert.equals(&amp;#34;hello-world&amp;#34;, utils.slugify(&amp;#34;hello, world!&amp;#34;)) end) end) describe(&amp;#34;utils.parse_token&amp;#34;, function() it(&amp;#34;returns nil for empty string&amp;#34;, function() assert.is_nil(utils.parse_token(&amp;#34;&amp;#34;)) end) it(&amp;#34;extracts bearer token&amp;#34;, function() local token = utils.parse_token(&amp;#34;Bearer abc123&amp;#34;) …  ]]></content></entry><entry><title>28 | 插件系统设计与扩展</title><url>/blog/openresty-28-plugin-system/</url><categories><category>OpenResty</category></categories><tags><tag>OpenResty</tag><tag>网关</tag><tag>插件系统</tag><tag>架构</tag></tags><content type="html"><![CDATA[  前几篇写了路由、鉴权、限流、熔断、改写这些具体插件。这篇来把插件系统本身做完整——自动加载、优先级排序、生命周期钩子，以及如何让别人能轻松写新插件。
插件加载机制 目标：把 plugins/ 目录下的所有 Lua 文件自动识别为插件，不需要手动注册。
-- core/plugin_runner.lua local M = {} -- 已加载的插件列表 local _plugins = {} local _loaded = false -- 扫描并加载所有插件 local function load_plugins() if _loaded then return end -- 内置插件列表（按文件名，实际项目可以用 lfs 扫描目录） local plugin_names = { &amp;#34;canary&amp;#34;, -- priority: 5 &amp;#34;ip_blacklist&amp;#34;, -- priority: 10 &amp;#34;rate_limit&amp;#34;, -- priority: 12 &amp;#34;jwt_auth&amp;#34;, -- priority: 20 &amp;#34;apikey_auth&amp;#34;, -- priority: 21 &amp;#34;circuit_breaker&amp;#34;, -- priority: 25 (access + log) &amp;#34;request_rewrite&amp;#34;, -- priority: 30 &amp;#34;logging&amp;#34;, -- priority: 90 (log only) } for _, name in ipairs(plugin_names) do local ok, plugin = pcall(require, &amp;#34;plugins.&amp;#34; .. name) if ok and plugin then _plugins[#_plugins + 1] = plugin ngx.log(ngx.DEBUG, &amp;#34;plugin loaded: &amp;#34;, name, &amp;#34; priority=&amp;#34;, plugin.priority or 100) else ngx.log(ngx.ERR, &amp;#34;failed to load plugin: &amp;#34;, name, …  ]]></content></entry><entry><title>27 | 请求/响应改写模块</title><url>/blog/openresty-27-request-rewrite/</url><categories><category>OpenResty</category></categories><tags><tag>OpenResty</tag><tag>网关</tag><tag>灰度发布</tag><tag>实战</tag></tags><content type="html"><![CDATA[  网关不只是&amp;quot;透明转发&amp;quot;，它还能在请求到达后端之前和响应返回客户端之前做各种改写。这篇来实现改写模块，并用它实现一个实用的功能：灰度发布。
请求头注入 鉴权通过后，网关知道了用户信息。把这些信息注入请求头，后端服务就不需要再做一次鉴权，直接读头就能拿到用户 ID、角色等信息。
-- plugins/request_rewrite.lua local Plugin = {} Plugin.name = &amp;#34;request_rewrite&amp;#34; Plugin.priority = 30 function Plugin:access(config) local user = ngx.ctx.user -- 注入用户信息 if user and config.inject_user ~= false then ngx.req.set_header(&amp;#34;X-User-Id&amp;#34;, tostring(user.id or &amp;#34;&amp;#34;)) ngx.req.set_header(&amp;#34;X-User-Name&amp;#34;, user.name or &amp;#34;&amp;#34;) ngx.req.set_header(&amp;#34;X-User-Roles&amp;#34;, user.roles and table.concat(user.roles, &amp;#34;,&amp;#34;) or &amp;#34;&amp;#34;) end -- 注入请求追踪 ID local trace_id = ngx.req.get_headers()[&amp;#34;x-trace-id&amp;#34;] if not trace_id then trace_id = ngx.var.request_id or ngx.md5(tostring(ngx.now()) .. ngx.var.remote_addr) end ngx.req.set_header(&amp;#34;X-Trace-Id&amp;#34;, trace_id) ngx.var.trace_id = trace_id -- 注入网关信息 ngx.req.set_header(&amp;#34;X-Forwarded-By&amp;#34;, &amp;#34;api-gateway&amp;#34;) …  ]]></content></entry><entry><title>26 | 熔断与降级模块</title><url>/blog/openresty-26-circuit-breaker/</url><categories><category>OpenResty</category></categories><tags><tag>OpenResty</tag><tag>熔断</tag><tag>网关</tag><tag>实战</tag></tags><content type="html"><![CDATA[  限流保护的是网关自身，熔断保护的是后端服务。当某个后端服务出现故障时，如果网关继续把请求转发过去，不仅请求会失败，还会占用连接资源，拖慢整个系统。熔断器的作用就是在检测到后端异常时，主动切断请求，给后端一个恢复的时间窗口。
熔断器三态模型 熔断器有三种状态，在这三种状态之间流转：
失败率超过阈值 关闭 ──────────────────→ 打开 (正常转发) (直接拒绝) ↑ │ │ 探测请求成功 │ 超过重置时间 └──────────────── 半开 ←──┘ (放行少量请求探测) 关闭（Closed）：正常状态，请求正常转发。统计失败率，超过阈值则转为打开 打开（Open）：熔断状态，所有请求直接返回降级响应，不转发到后端 半开（Half-Open）：探测状态，打开一段时间后自动进入。放行少量请求探测后端是否恢复，成功则转为关闭，失败则重新转为打开 用 ngx.shared.DICT 实现熔断 -- plugins/circuit_breaker.lua local Plugin = {} Plugin.name = &amp;#34;circuit_breaker&amp;#34; Plugin.priority = 15 -- 状态常量 local STATE_CLOSED = &amp;#34;closed&amp;#34; local STATE_OPEN = &amp;#34;open&amp;#34; local STATE_HALF_OPEN = &amp;#34;half_open&amp;#34; local stats = ngx.shared.circuit_breaker -- 获取熔断器状态 local function get_state(service_id) return stats:get(&amp;#34;state:&amp;#34; .. service_id) or STATE_CLOSED end -- 记录请求结果 local function record_result(service_id, success) local window = 60 -- 统计窗口：60 秒 if success then stats:incr(&amp;#34;success:&amp;#34; .. service_id, 1, 0, window) else stats:incr(&amp;#34;failure:&amp;#34; …  ]]></content></entry><entry><title>25 | 限流模块</title><url>/blog/openresty-25-rate-limit/</url><categories><category>OpenResty</category></categories><tags><tag>OpenResty</tag><tag>限流</tag><tag>Redis</tag><tag>网关</tag></tags><content type="html"><![CDATA[  鉴权解决了&amp;quot;谁能访问&amp;quot;的问题，限流解决&amp;quot;能访问多少次&amp;quot;的问题。没有限流的 API 网关是不完整的——不管是防止恶意刷接口，还是保护后端服务不被突发流量击垮，限流都是不可或缺的。
三种限流算法 固定窗口 把时间切成固定大小的窗口（如每分钟），统计窗口内的请求数，超过阈值则拒绝。
时间轴: |--- 0:00 ~ 0:01 ---|--- 0:01 ~ 0:02 ---| [请求数: 95] [请求数: 3] ↑ 在 0:00:59 发 5 个请求不会超限（95+5=100） 但 0:01:00 再发 5 个也不超限（3+5=8） 窗口切换后计数清零，临界点可能出现 2 倍突刺 优点：实现简单，Redis 里一个 INCR + EXPIRE 就够。 缺点：窗口切换时的临界突刺——在 0:00:59 发 100 个请求，0:01:00 再发 100 个，1 秒内实际处理了 200 个请求。
滑动窗口 用精确的时间范围（当前时间往前 N 秒）统计请求数，没有固定边界，消除了临界突刺。
当前时间: 0:01:30，窗口大小: 60s 统计范围: 0:00:30 ~ 0:01:30 内的所有请求 Redis 实现：用 Sorted Set，以时间戳为 score，每次请求 ZADD，统计时 ZCOUNT 指定时间范围。
优点：没有窗口边界问题，限流更平滑。 缺点：每个请求写一条记录，高并发时内存占用高。
令牌桶 有一个固定容量的桶，系统以固定速率往桶里放令牌。每个请求消耗一个令牌，桶空了就拒绝。令牌可以积累，所以允许一定程度的突发流量。
桶容量: 100，填充速率: 10 个/秒 桶里初始 100 个令牌 → 可以瞬间处理 100 个请求（消耗所有令牌） → 之后每秒最多处理 10 个（稳定速率） → 如果某秒只来了 5 个请求，桶里积累 5 个，供后续突发使用 优点：允许突发，限制长期平均速率，对用户体验友好。 缺点：实现稍复杂，需要计算令牌数量。
固定窗口限流实现 最简单的实现，适合大多数场景：
-- Redis Lua 脚本（原子操作） local FIXED_WINDOW_SCRIPT = [[ local key = KEYS[1] local limit = tonumber(ARGV[1]) local window = …  ]]></content></entry><entry><title>24 | 鉴权模块：API Key + HMAC 签名</title><url>/blog/openresty-24-apikey-hmac/</url><categories><category>OpenResty</category></categories><tags><tag>OpenResty</tag><tag>HMAC</tag><tag>API Key</tag><tag>鉴权</tag><tag>网关</tag></tags><content type="html"><![CDATA[  JWT 适合用户身份认证，但服务间调用（机器对机器）通常用 API Key + HMAC 签名。这种方式不需要登录流程，调用方持有 API Key 和 Secret，每次请求用 Secret 对请求内容签名，网关验证签名的合法性。
API Key 设计规范 一对 API 凭证包含两部分：
API Key（app_key）：公开标识符，类似用户名，用于查找对应的 Secret。格式示例：ak_live_a1b2c3d4e5f6 API Secret（app_secret）：私密密钥，类似密码，只有调用方知道，用于生成签名。网关侧存储其哈希值。 前缀: ak_ -- api key 环境: live_ / test_ -- 区分生产和测试 随机串: a1b2c3d4e5f6 -- 32位随机十六进制 在 Redis 里存储 API Key 信息：
# key: apikey:&amp;lt;app_key&amp;gt; # value: JSON redis-cli set &amp;#34;apikey:ak_live_a1b2c3d4e5f6&amp;#34; &amp;#39;{ &amp;#34;app_key&amp;#34;: &amp;#34;ak_live_a1b2c3d4e5f6&amp;#34;, &amp;#34;app_secret_hash&amp;#34;: &amp;#34;&amp;lt;bcrypt hash of secret&amp;gt;&amp;#34;, &amp;#34;app_name&amp;#34;: &amp;#34;order-service&amp;#34;, &amp;#34;permissions&amp;#34;: [&amp;#34;read&amp;#34;, &amp;#34;write&amp;#34;], &amp;#34;rate_limit&amp;#34;: 1000, &amp;#34;enabled&amp;#34;: true }&amp;#39; 注意：存储的是 Secret 的哈希值，不是明文。但 HMAC 验证需要原始 Secret，所以实际上有两种存储方案：
存明文 Secret：简单，但有泄露风险 存密文，签名时重新计算：更安全，但需要在签名协议中让调用方传 Secret 原文……这显然不合理 实践中，API Secret 通常存明文但加密保存（用 AES 加密存储，读取时解密），或者接受&amp;quot;内部系统信任边界&amp;quot;的前提直接存明文。下文示例用明文存储以保持清晰。
HMAC-SHA256 …  ]]></content></entry><entry><title>23 | 鉴权模块：JWT 认证</title><url>/blog/openresty-23-jwt-auth/</url><categories><category>OpenResty</category></categories><tags><tag>OpenResty</tag><tag>JWT</tag><tag>鉴权</tag><tag>网关</tag></tags><content type="html"><![CDATA[  路由模块搭好了，下一步是鉴权。没有通过鉴权的请求，不应该打到后端服务。这篇实现 JWT 鉴权插件。
JWT 原理与结构 JWT（JSON Web Token）是一种自包含的令牌格式。一个 JWT 由三部分组成，用 . 分隔：
eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJzdWIiOiIxMjMiLCJuYW1lIjoiYWxpY2UiLCJleHAiOjE3MDk5OTk5OTl9.SflKxwRJSMeKKF2QT4fwpMeJf36POk6yJV_adQssw5c Header（第一段）：Base64 编码的 JSON，包含算法类型（如 HS256） Payload（第二段）：Base64 编码的 JSON，包含声明（Claims），如用户 ID、过期时间 Signature（第三段）：用密钥对 Header + Payload 做 HMAC 签名，防篡改 标准 Payload 字段：
字段 含义 sub Subject，通常是用户 ID exp Expiration，过期时间（Unix 时间戳） iat Issued At，签发时间 jti JWT ID，唯一标识，用于撤销 iss Issuer，签发者 JWT 的验证逻辑：用同样的密钥重新计算签名，与 Token 中的签名对比——一致则说明 Token 未被篡改，再检查 exp 是否过期。
安装 lua-resty-jwt opm get SkyLothar/lua-resty-jwt 实现 JWT 鉴权插件 -- plugins/jwt_auth.lua local jwt = require &amp;#34;resty.jwt&amp;#34; local cjson = require &amp;#34;cjson&amp;#34; local response = require &amp;#34;lib.response&amp;#34; local Plugin = {} Plugin.name = &amp;#34;jwt_auth&amp;#34; Plugin.priority = 20 -- 鉴权在限流之后执行 -- 从请求头提取 Token local function extract_token() local auth_header = …  ]]></content></entry><entry><title>22 | 路由模块：动态路由</title><url>/blog/openresty-22-dynamic-routing/</url><categories><category>OpenResty</category></categories><tags><tag>OpenResty</tag><tag>网关</tag><tag>路由</tag></tags><content type="html"><![CDATA[  架构设计好了，这篇来实现第一个核心模块：路由。路由模块要解决两个问题——怎么匹配请求，怎么找到对应的后端服务。
路由匹配策略 路由匹配通常有三种方式，按精确度从高到低：
精确匹配：/api/v1/user/profile 只匹配这一个路径 前缀匹配：/api/v1/user 匹配所有以此开头的路径 正则匹配：/api/v[0-9]+/user 用正则表达式匹配 匹配优先级：精确 &amp;gt; 前缀（长的优先） &amp;gt; 正则。
-- core/router.lua local cjson = require &amp;#34;cjson&amp;#34; local M = {} -- 路由表（从共享内存加载） local function get_routes() local cache = ngx.shared.routes local data = cache:get(&amp;#34;routes&amp;#34;) if not data then return {} end return cjson.decode(data) or {} end -- 路由匹配 function M.match(uri, method) local routes = get_routes() local matched = nil local matched_len = 0 for _, route in ipairs(routes) do if not route.enabled then goto continue end -- 方法过滤 if route.methods then local method_ok = false for _, m in ipairs(route.methods) do if m == method then method_ok = true; break end end if not method_ok then goto continue end end -- 精确匹配 if route.path == uri then return route end -- 前缀匹配（取最长匹配） if route.path_prefix then local prefix = route.path_prefix if uri:sub(1, #prefix) == prefix …  ]]></content></entry><entry><title>21 | 网关架构设计</title><url>/blog/openresty-21-gateway-architecture/</url><categories><category>OpenResty</category></categories><tags><tag>OpenResty</tag><tag>网关</tag><tag>架构设计</tag></tags><content type="html"> 前四个阶段把 OpenResty 的语言基础、核心 API、存储集成都学完了。从这篇开始，我们用这些知识从零手写一个具备生产能力的 API 网关。
在写代码之前，先把架构想清楚。好的架构让后续扩展事半功倍，坏的架构则让每次改动都牵一发动全身。
网关的核心职责 API 网关站在客户端和后端服务之间，承担以下职责：
流量管控
路由：根据请求路径、方法、Header 等分发到不同后端服务 负载均衡：在多个后端实例之间分配流量 限流：保护后端不被流量击垮 安全
鉴权：验证请求方身份（JWT、API Key、OAuth） 认证信息传递：把用户信息注入请求头传给后端 防攻击：IP 黑名单、请求体大小限制 可观测性
日志：记录每个请求的完整信息 监控指标：请求量、延迟、错误率 链路追踪：Trace ID 贯穿全链路 协议与格式转换
请求/响应改写：添加/删除 Header、修改路径 灰度发布：按规则把部分流量路由到新版本 插件化架构 如果把路由、鉴权、限流、日志全部写死在主逻辑里，代码很快会变成一团乱麻。插件化架构把每个功能独立成一个插件，主流程只负责按顺序调用插件。
请求到达 │ ▼ ┌─────────────────────────────────┐ │ 插件执行管道（access 阶段） │ │ 1. ip_blacklist（IP 黑名单） │ │ 2. rate_limit（限流） │ │ 3. auth（鉴权） │ │ 4. request_rewrite（请求改写） │ └────────────────┬────────────────┘ │ ▼ 转发到后端服务 │ ▼ ┌─────────────────────────────────┐ │ 插件执行管道（response 阶段） │ │ 5. response_rewrite（响应改写） │ │ 6. logging（日志记录） │ └─────────────────────────────────┘ 每个插件的接口统一：
-- 插件接口约定 local Plugin = {} -- 插件名称 Plugin.name = &amp;amp;#34;my_plugin&amp;amp;#34; -- 插件优先级（数字越小越先执行） Plugin.priority = 100 -- access 阶段（处理请求） function …</content></entry><entry><title>20 | 日志系统设计</title><url>/blog/openresty-20-logging/</url><categories><category>OpenResty</category></categories><tags><tag>OpenResty</tag><tag>日志</tag><tag>Kafka</tag><tag>实战</tag></tags><content type="html"><![CDATA[  日志是系统的眼睛。一个好的日志系统，不只是把信息写到文件里，还要考虑：格式是否方便机器解析、是否影响请求性能、如何与下游日志平台对接。这篇来系统地聊 OpenResty 的日志设计。
结构化日志输出 传统的日志是人可读的文本，比如 Nginx 默认的 combined 格式：
192.168.1.1 - - [10/Mar/2026:10:00:00 +0800] &amp;#34;GET /api/users HTTP/1.1&amp;#34; 200 1234 这种格式对人友好，但对机器不友好——解析时需要正则匹配，字段顺序固定，扩展字段麻烦。
结构化日志用 JSON 格式，每行一个完整的日志对象：
{&amp;#34;time&amp;#34;:&amp;#34;2026-03-10T10:00:00+08:00&amp;#34;,&amp;#34;method&amp;#34;:&amp;#34;GET&amp;#34;,&amp;#34;uri&amp;#34;:&amp;#34;/api/users&amp;#34;,&amp;#34;status&amp;#34;:200,&amp;#34;body_bytes&amp;#34;:1234,&amp;#34;request_time&amp;#34;:0.023,&amp;#34;remote_addr&amp;#34;:&amp;#34;192.168.1.1&amp;#34;,&amp;#34;user_id&amp;#34;:&amp;#34;123&amp;#34;,&amp;#34;trace_id&amp;#34;:&amp;#34;abc-xyz&amp;#34;} 字段清晰，扩展方便，直接被 Elasticsearch、Loki 等平台摄取。
自定义 access_log 格式 Nginx 的 log_format 指令支持自定义日志格式。结合 OpenResty 的变量，可以输出结构化的 JSON 日志：
http { log_format json_log escape=json &amp;#39;{&amp;#39; &amp;#39;&amp;#34;time&amp;#34;:&amp;#34;$time_iso8601&amp;#34;,&amp;#39; &amp;#39;&amp;#34;remote_addr&amp;#34;:&amp;#34;$remote_addr&amp;#34;,&amp;#39; &amp;#39;&amp;#34;method&amp;#34;:&amp;#34;$request_method&amp;#34;,&amp;#39; &amp;#39;&amp;#34;uri&amp;#34;:&amp;#34;$uri&amp;#34;,&amp;#39; …  ]]></content></entry><entry><title>19 | 本地缓存与多级缓存</title><url>/blog/openresty-19-cache/</url><categories><category>OpenResty</category></categories><tags><tag>OpenResty</tag><tag>缓存</tag><tag>Redis</tag><tag>实战</tag></tags><content type="html"><![CDATA[  缓存是高并发系统的核心优化手段。OpenResty 的独特之处在于它有多层缓存可以使用：进程内的 LRU 缓存、worker 级的共享内存、以及外部的 Redis。合理组合这三层，可以在极低延迟下扛住非常高的并发。
ngx.shared.DICT 进阶 第 12 篇已经介绍了 ngx.shared.DICT 的基础用法，这里补充几个进阶场景。
带标志位的缓存 set、get 等方法支持一个可选的 flags 参数（uint32 整数），可以用来存储元信息：
local cache = ngx.shared.my_cache -- 用 flags 存数据类型标记（1=string, 2=number, 3=json） cache:set(&amp;#34;config&amp;#34;, &amp;#39;{&amp;#34;debug&amp;#34;:true}&amp;#39;, 300, 3) -- 第4个参数是 flags local val, flags = cache:get(&amp;#34;config&amp;#34;) if flags == 3 then val = cjson.decode(val) end 分布式锁（简易版） add 方法的原子性可以用来实现简单的分布式锁：
local lock_key = &amp;#34;lock:cache_rebuild:&amp;#34; .. resource_id local ok, err, forcible = ngx.shared.locks:add(lock_key, 1, 10) -- 10 秒超时 if not ok then -- 已有其他请求在重建缓存，等待后读取 ngx.sleep(0.1) return get_from_cache(resource_id) end -- 拿到锁，重建缓存 local data = fetch_from_db(resource_id) ngx.shared.my_cache:set(resource_id, cjson.encode(data), 300) -- 释放锁 ngx.shared.locks:delete(lock_key) return data 内存占满时的行为 当 ngx.shared.DICT 内存用满，新写入时有两种结果：
如果有可以 LRU 淘汰的 key，自动淘汰旧 key（set 返回的第三 …  ]]></content></entry><entry><title>18 | HTTP 客户端</title><url>/blog/openresty-18-http-client/</url><categories><category>OpenResty</category></categories><tags><tag>OpenResty</tag><tag>HTTP</tag><tag>实战</tag></tags><content type="html"><![CDATA[  在 API 网关的场景里，OpenResty 不只是接收请求，很多时候还需要主动调用其他服务——验证 Token、查询用户信息、发送 Webhook 通知。这些都需要 HTTP 客户端能力。
lua-resty-http lua-resty-http 是目前 OpenResty 生态里最成熟的 HTTP 客户端库，由 pintsized 维护。它基于 cosocket，完全非阻塞。
安装（如果未内置）：
opm get ledgetech/lua-resty-http 基本用法 简单 GET 请求 local httpc = require(&amp;#34;resty.http&amp;#34;).new() -- request_uri 是最简洁的接口，适合简单请求 local res, err = httpc:request_uri(&amp;#34;https://api.example.com/users/123&amp;#34;, { method = &amp;#34;GET&amp;#34;, headers = { [&amp;#34;Authorization&amp;#34;] = &amp;#34;Bearer &amp;#34; .. token, [&amp;#34;Accept&amp;#34;] = &amp;#34;application/json&amp;#34;, }, ssl_verify = false, -- 开发环境可关闭证书验证，生产环境建议开启 }) if not res then ngx.log(ngx.ERR, &amp;#34;request failed: &amp;#34;, err) return ngx.exit(500) end ngx.log(ngx.INFO, &amp;#34;status: &amp;#34;, res.status) ngx.say(res.body) POST 请求（JSON Body） local httpc = require(&amp;#34;resty.http&amp;#34;).new() local cjson = require &amp;#34;cjson&amp;#34; local payload = cjson.encode({ user_id = 123, action = &amp;#34;login&amp;#34;, ip = ngx.var.remote_addr, }) local res, err = …  ]]></content></entry><entry><title>17 | 连接 MySQL</title><url>/blog/openresty-17-mysql/</url><categories><category>OpenResty</category></categories><tags><tag>OpenResty</tag><tag>MySQL</tag><tag>实战</tag></tags><content type="html"><![CDATA[  Redis 适合存临时数据和缓存，但涉及结构化数据、需要事务保证、需要复杂查询的场景，还是得上 MySQL。这篇来讲 OpenResty 怎么连接 MySQL。
lua-resty-mysql 基本使用 lua-resty-mysql 同样是 OpenResty 官方库，基于 cosocket，完全非阻塞。OpenResty 发行版内置，直接 require 即可。
连接 local mysql = require &amp;#34;resty.mysql&amp;#34; local db, err = mysql:new() if not db then ngx.log(ngx.ERR, &amp;#34;failed to create mysql: &amp;#34;, err) return ngx.exit(500) end -- 设置超时 db:set_timeout(1000) -- 单个超时值，单位毫秒 -- 连接数据库 local ok, err, errcode, sqlstate = db:connect({ host = &amp;#34;127.0.0.1&amp;#34;, port = 3306, database = &amp;#34;myapp&amp;#34;, user = &amp;#34;appuser&amp;#34;, password = &amp;#34;secret&amp;#34;, charset = &amp;#34;utf8mb4&amp;#34;, max_packet_size = 1024 * 1024, -- 1MB }) if not ok then ngx.log(ngx.ERR, &amp;#34;mysql connect failed: &amp;#34;, err, &amp;#34;, code: &amp;#34;, errcode) return ngx.exit(500) end 执行查询 -- 查询返回结果集 local res, err, errcode, sqlstate = db:query(&amp;#34;SELECT id, name, email FROM users LIMIT 10&amp;#34;) if not res then ngx.log(ngx.ERR, &amp;#34;query failed: &amp;#34;, err) return end -- res 是一个数组，每个元素是一行数据（table，键为列 …  ]]></content></entry><entry><title>16 | 连接 Redis：实战篇</title><url>/blog/openresty-16-redis-advanced/</url><categories><category>OpenResty</category></categories><tags><tag>OpenResty</tag><tag>Redis</tag><tag>实战</tag></tags><content type="html"><![CDATA[  上一篇打好了 Redis 连接的基础。这篇来看实战场景：Pipeline 批量操作、Pub/Sub 订阅，以及两个高频业务需求——分布式 Session 和 Token 黑名单。
Pipeline 批量操作 每次 Redis 命令都是一次网络往返（RTT）。如果需要执行 10 条命令，串行执行就是 10 次 RTT。Pipeline 把多个命令打包成一次网络请求发出去，一次性拿回所有结果，大幅减少网络开销。
local redis = require &amp;#34;resty.redis&amp;#34; local red = redis:new() red:set_timeouts(1000, 1000, 1000) red:connect(&amp;#34;127.0.0.1&amp;#34;, 6379) -- 开启 pipeline red:init_pipeline() -- 这些命令不会立即发送，先缓存在本地 red:set(&amp;#34;key1&amp;#34;, &amp;#34;val1&amp;#34;) red:set(&amp;#34;key2&amp;#34;, &amp;#34;val2&amp;#34;) red:get(&amp;#34;key1&amp;#34;) red:get(&amp;#34;key2&amp;#34;) red:incr(&amp;#34;counter&amp;#34;) -- 一次性发送所有命令，返回结果数组 local results, err = red:commit_pipeline() if not results then ngx.log(ngx.ERR, &amp;#34;pipeline failed: &amp;#34;, err) red:set_keepalive(10000, 100) return end -- results[1] 对应第一条命令的结果，以此类推 ngx.say(&amp;#34;set key1: &amp;#34;, results[1]) -- OK ngx.say(&amp;#34;set key2: &amp;#34;, results[2]) -- OK ngx.say(&amp;#34;get key1: &amp;#34;, results[3]) -- val1 ngx.say(&amp;#34;get key2: &amp;#34;, results[4]) -- val2 ngx.say(&amp;#34;counter: &amp;#34;, …  ]]></content></entry><entry><title>15 | 连接 Redis：基础篇</title><url>/blog/openresty-15-redis-basic/</url><categories><category>OpenResty</category></categories><tags><tag>OpenResty</tag><tag>Lua</tag><tag>Redis</tag></tags><content type="html"><![CDATA[  从这篇开始进入第四阶段：中间件与存储。Redis 是 OpenResty 项目里最常见的搭档，几乎所有需要缓存、计数、会话存储的场景都少不了它。这篇先把基础打牢：如何连接、常用命令怎么用、连接池怎么配置。
安装 lua-resty-redis lua-resty-redis 是 OpenResty 官方提供的 Redis 客户端，基于 cosocket，完全非阻塞。OpenResty 发行版通常已经内置，不需要单独安装。
检查是否可用：
local ok, redis = pcall(require, &amp;#34;resty.redis&amp;#34;) if ok then print(&amp;#34;lua-resty-redis available&amp;#34;) end 如果没有，可以用 OPM（OpenResty Package Manager）安装：
opm get openresty/lua-resty-redis 基本连接 local redis = require &amp;#34;resty.redis&amp;#34; local red = redis:new() -- 设置超时（连接超时、读超时、发送超时，单位毫秒） red:set_timeouts(1000, 1000, 1000) -- 连接 local ok, err = red:connect(&amp;#34;127.0.0.1&amp;#34;, 6379) if not ok then ngx.log(ngx.ERR, &amp;#34;failed to connect redis: &amp;#34;, err) return ngx.exit(500) end -- 如果 Redis 设置了密码 local ok, err = red:auth(&amp;#34;your_password&amp;#34;) if not ok then ngx.log(ngx.ERR, &amp;#34;redis auth failed: &amp;#34;, err) return ngx.exit(500) end -- 选择数据库（默认是 0） red:select(1) 常用命令 lua-resty-redis 的 API 设计很直接——Redis 命令名就是方法名，参数一一对应。
String 操作 -- SET key value [EX seconds] [NX] …  ]]></content></entry><entry><title>14 | 定时器与后台任务</title><url>/blog/openresty-14-timers/</url><categories><category>OpenResty</category></categories><tags><tag>OpenResty</tag><tag>Lua</tag><tag>定时器</tag></tags><content type="html"><![CDATA[  上一篇提到，在 log_by_lua 阶段不能用 cosocket，但可以用 ngx.timer.at 把任务推到后台异步执行。这篇专门来讲 OpenResty 的定时器机制——它是实现后台任务的核心工具。
ngx.timer.at：一次性定时器 ngx.timer.at(delay, callback, ...) 在 delay 秒后执行 callback。
-- 0 秒后立即执行（下一个事件循环） ngx.timer.at(0, function(premature) if premature then -- premature = true 表示 nginx 正在关闭（worker 退出时） -- 应该尽快清理并返回，不要做耗时操作 return end ngx.log(ngx.INFO, &amp;#34;timer fired!&amp;#34;) end) -- 5 秒后执行 ngx.timer.at(5, function(premature) if premature then return end -- 做一些事情 end) -- 传递额外参数 local function report_log(premature, log_data) if premature then return end -- 上报 log_data ngx.log(ngx.INFO, &amp;#34;reporting: &amp;#34;, log_data.uri) end ngx.timer.at(0, report_log, { uri = ngx.var.uri, status = ngx.status, time = ngx.now(), }) 关于 premature 参数： 这是 OpenResty 传给所有定时器回调的第一个参数。当 nginx worker 正在关闭（收到 SIGTERM 或 SIGQUIT）时，所有未触发的定时器会被强制触发，premature 置为 true。回调里应该检查这个参数，在 premature = true 时优雅退出，不要再发起新的 I/O。
ngx.timer.every：周期定时器 ngx.timer.every(interval, callback, ...) 每隔 interval 秒重复执行一次。
-- 每 60 秒同步一次配置 local …  ]]></content></entry><entry><title>13 | 协程与非阻塞 I/O</title><url>/blog/openresty-13-coroutine-io/</url><categories><category>OpenResty</category></categories><tags><tag>OpenResty</tag><tag>Lua</tag><tag>协程</tag><tag>cosocket</tag></tags><content type="html"><![CDATA[  很多人用 OpenResty 一段时间后会有一个困惑：明明代码里写了连接 Redis、发起 HTTP 请求这些操作，但性能还是很高，没有感受到明显的阻塞。这是为什么？
答案就在这篇文章里：OpenResty 的协程模型和 cosocket 机制。理解这个模型，你才能真正知道 OpenResty 的高并发是怎么实现的，以及哪些操作会破坏这个模型。
OpenResty 的协程模型 先从 Nginx 说起。Nginx 用事件驱动模型处理连接：一个 worker 进程可以同时维护成千上万条连接，靠的是 epoll（Linux）这样的 I/O 多路复用机制。当某个连接的数据还没到时，worker 不等待，去处理其他连接，等内核通知&amp;quot;数据到了&amp;quot;再回来处理。
OpenResty 在这个基础上加了 LuaJIT 和协程。每个 HTTP 请求对应一个 Lua 协程。当协程需要做 I/O（比如查 Redis、发 HTTP 请求），它会：
发出 I/O 请求 把自己挂起，把控制权交还给 Nginx 的事件循环 Nginx 继续处理其他请求 I/O 完成后，Nginx 事件循环唤醒这个协程 协程从挂起点继续执行 从你的代码角度看，这些都是透明的——你写的是同步风格的代码，但底层实际上是异步非阻塞的。这就是 OpenResty 的核心魔法。
-- 看起来是同步代码，实际是非阻塞的 local red = redis:new() red:connect(&amp;#34;127.0.0.1&amp;#34;, 6379) -- 挂起，等连接建立 local val = red:get(&amp;#34;key&amp;#34;) -- 挂起，等 Redis 返回 ngx.say(val) cosocket：协程感知的 socket cosocket 是 OpenResty 自己实现的网络 socket，名字里的 &amp;ldquo;co&amp;rdquo; 来自 coroutine（协程）。它的特别之处在于：当需要等待网络 I/O 时，它会自动挂起当前协程，不占用 CPU，等数据到了再自动恢复。
所有 lua-resty-* 库（resty.redis、resty.http 等）底层都用了 cosocket。
手动使用 cosocket 虽然大部分时候你用的是封装好的库，但了解底层接口有助于理解原理： …  ]]></content></entry><entry><title>12 | 变量与上下文共享</title><url>/blog/openresty-12-variables-context/</url><categories><category>OpenResty</category></categories><tags><tag>OpenResty</tag><tag>Lua</tag><tag>共享内存</tag><tag>ngx.ctx</tag></tags><content type="html"><![CDATA[  在 OpenResty 里写业务逻辑，经常会遇到一个问题：数据在哪里存？不同阶段之间怎么传数据？多个请求之间能共享什么？
这些问题的答案，取决于你选择哪种存储方式。OpenResty 提供了几个不同作用域的存储机制，搞清楚它们的边界，能避开很多诡异的 bug。
ngx.var：读写 Nginx 变量 ngx.var 提供对 Nginx 内置变量和自定义变量的读写访问。
读取内置变量 -- 常用内置变量 local host = ngx.var.host -- 请求的 Host local uri = ngx.var.uri -- 请求 URI（不含 query string） local request_uri = ngx.var.request_uri -- 完整请求路径（含 query string） local method = ngx.var.request_method -- GET / POST / ... local remote_addr = ngx.var.remote_addr -- 客户端 IP local scheme = ngx.var.scheme -- http 或 https local server_port = ngx.var.server_port -- 服务端口 local http_referer = ngx.var.http_referer -- Referer 头（http_ 前缀访问请求头） local arg_page = ngx.var.arg_page -- query string 中的 page 参数 http_ 前缀可以访问任意请求头：ngx.var.http_x_forwarded_for 对应 X-Forwarded-For 头（连字符换成下划线，统一小写）。
arg_ 前缀可以访问 query string 参数：ngx.var.arg_id 对应 ?id=123。
自定义变量 在 nginx.conf 里用 set 指令声明变量，Lua 里再读写：
location /api/ { set $user_id &amp;#34;&amp;#34;; -- 声明变量（必须先声明） set $auth_status &amp;#34;0&amp;#34;; access_by_lua_block { -- …  ]]></content></entry><entry><title>11 | 响应处理：控制一切输出</title><url>/blog/openresty-11-response-handling/</url><categories><category>OpenResty</category></categories><tags><tag>OpenResty</tag><tag>Lua</tag><tag>响应处理</tag></tags><content type="html"><![CDATA[  上一篇把请求输入的读取方式讲完了。这篇来看另一半——如何控制响应输出。一个请求的生命周期里，响应这一侧同样有很多细节值得梳理。
设置响应头与状态码 响应头和状态码必须在发送 body 之前设置，一旦第一次调用 ngx.say 或 ngx.print，响应头就会自动发出，之后再修改就没有效果了。
content_by_lua_block { -- 设置状态码 ngx.status = 201 -- 设置响应头 ngx.header[&amp;#34;Content-Type&amp;#34;] = &amp;#34;application/json; charset=utf-8&amp;#34; ngx.header[&amp;#34;X-Request-Id&amp;#34;] = &amp;#34;abc-123&amp;#34; ngx.header[&amp;#34;Cache-Control&amp;#34;] = &amp;#34;no-cache, no-store&amp;#34; -- 设置多值响应头 ngx.header[&amp;#34;Set-Cookie&amp;#34;] = { &amp;#34;session_id=abc123; Path=/; HttpOnly&amp;#34;, &amp;#34;theme=dark; Path=/; Max-Age=86400&amp;#34; } -- 删除某个响应头 ngx.header[&amp;#34;X-Powered-By&amp;#34;] = nil -- 此时才发送 body（头也随之发出） ngx.say(&amp;#39;{&amp;#34;code&amp;#34;:0}&amp;#39;) } 常用状态码常量，不用背数字：
ngx.HTTP_OK -- 200 ngx.HTTP_CREATED -- 201 ngx.HTTP_NO_CONTENT -- 204 ngx.HTTP_MOVED_PERMANENTLY -- 301 ngx.HTTP_MOVED_TEMPORARILY -- 302 ngx.HTTP_NOT_MODIFIED -- 304 ngx.HTTP_BAD_REQUEST -- 400 ngx.HTTP_UNAUTHORIZED -- 401 ngx.HTTP_FORBIDDEN -- 403 ngx.HTTP_NOT_FOUND -- 404 ngx.HTTP_INTERNAL_SERVER_ERROR -- 500 …  ]]></content></entry><entry><title>10 | 请求处理：读取一切输入</title><url>/blog/openresty-10-request-handling/</url><categories><category>OpenResty</category></categories><tags><tag>OpenResty</tag><tag>Lua</tag><tag>请求处理</tag></tags><content type="html"><![CDATA[  一个 HTTP 请求能携带的信息很多：URL 里的查询参数、POST 表单数据、各种请求头、JSON Body……OpenResty 提供了一套完整的 API 来读取这些输入。这篇文章逐一讲清楚每种输入的读取方式，以及各种坑。
读取查询参数 URL 里 ? 后面的部分就是查询参数，比如 /api/users?page=2&amp;amp;size=10&amp;amp;tag=lua&amp;amp;tag=openresty。
local args, err = ngx.req.get_uri_args() if not args then ngx.log(ngx.ERR, &amp;#34;failed to get uri args: &amp;#34;, err) return end -- 读取单个参数 local page = args.page or &amp;#34;1&amp;#34; local size = args[&amp;#34;size&amp;#34;] or &amp;#34;10&amp;#34; -- 参数有多个同名值时，是一个数组 local tags = args.tag if type(tags) == &amp;#34;table&amp;#34; then -- tags = {&amp;#34;lua&amp;#34;, &amp;#34;openresty&amp;#34;} for _, tag in ipairs(tags) do ngx.say(&amp;#34;tag: &amp;#34;, tag) end elseif type(tags) == &amp;#34;string&amp;#34; then ngx.say(&amp;#34;tag: &amp;#34;, tags) end get_uri_args() 默认最多返回 100 个参数，超出的会被截断。如果你的接口参数可能很多，可以传入上限值：
local args = ngx.req.get_uri_args(200) -- 最多返回 200 个 local args = ngx.req.get_uri_args(0) -- 0 表示不限制（谨慎使用） 注意：所有参数值都是字符串，args.page 得到的是 &amp;quot;2&amp;quot; 而不是 2，需要用 tonumber() 转换。
读取 POST 参数 Content-Type: application/x-www-form-urlencoded 格式的 POST …  ]]></content></entry><entry><title>09 | ngx. 核心 API 全览</title><url>/blog/openresty-09-ngx-api/</url><categories><category>OpenResty</category></categories><tags><tag>OpenResty</tag><tag>Lua</tag><tag>ngx API</tag></tags><content type="html"><![CDATA[  前两个阶段我们把 Lua 语言本身搞清楚了。从这篇开始进入第三阶段——OpenResty 核心 API。这些 API 是你和 Nginx 打交道的直接工具，几乎每个请求处理逻辑里都会用到。
输出响应 ngx.say 和 ngx.print 这是最基础的输出函数，区别只有一个：ngx.say 末尾自动加换行，ngx.print 不加。
content_by_lua_block { ngx.say(&amp;#34;Hello, World!&amp;#34;) -- 输出 &amp;#34;Hello, World!\n&amp;#34; ngx.print(&amp;#34;no newline here&amp;#34;) -- 输出 &amp;#34;no newline here&amp;#34; ngx.say(&amp;#34;line 1&amp;#34;, &amp;#34;line 2&amp;#34;) -- 可以传多个参数，依次输出 } 两个函数都接受多个参数，效果等同于拼接后输出。传 nil 会被忽略，传 table 会报错。
ngx.send_headers 默认情况下，第一次调用 ngx.say 或 ngx.print 时响应头会自动发送。如果需要提前发送头，可以显式调用：
ngx.status = 200 ngx.header[&amp;#34;Content-Type&amp;#34;] = &amp;#34;application/json&amp;#34; ngx.send_headers() -- 之后可以分块输出 body ngx.say(&amp;#39;{&amp;#34;status&amp;#34;: &amp;#34;ok&amp;#34;}&amp;#39;) ngx.flush 强制把缓冲区里的内容发送给客户端（流式响应时用）：
for i = 1, 5 do ngx.say(&amp;#34;chunk &amp;#34; .. i) ngx.flush(true) -- true 表示同步等待发送完成 ngx.sleep(0.5) end 控制响应状态 ngx.status ngx.status = 404 ngx.say(&amp;#34;Not Found&amp;#34;) ngx.status 必须在发送响应头之前设置，否则不生效。
ngx.exit 立即结束请求处理，返回指定状态码：
-- 返回 403 并终止 ngx.exit(403) -- 返回 200（正常结束） ngx.exit(ngx.OK) …  ]]></content></entry><entry><title>08 | LuaJIT 与性能</title><url>/blog/openresty-08-luajit-performance/</url><categories><category>OpenResty</category></categories><tags><tag>OpenResty</tag><tag>Lua</tag><tag>LuaJIT</tag><tag>性能</tag></tags><content type="html"> OpenResty 之所以能在高并发场景下打出不俗的性能，Nginx 的事件驱动模型是一方面，另一方面就是 LuaJIT。这篇文章来讲清楚 LuaJIT 是什么、它为什么快、以及在实际开发中有哪些性能陷阱值得注意。
LuaJIT 与标准 Lua 的区别 标准 Lua（PUC-Rio Lua）是一个解释器，每次执行都要把 Lua 代码翻译成字节码再逐条解释运行。
LuaJIT 则是一个带有即时编译（JIT，Just-In-Time Compilation）能力的实现。它能在运行时分析热点代码，把频繁执行的路径直接编译成机器码，性能可以接近 C 语言。
除了 JIT，LuaJIT 还有：
更快的解释器：即便 JIT 没有触发，LuaJIT 的解释器本身也比标准 Lua 快 2～5 倍 FFI 库：可以直接调用 C 函数和使用 C 数据结构，无需编写 C 扩展 更好的数字处理：LuaJIT 默认使用双精度浮点，数学运算路径更短 在 OpenResty 里，LuaJIT 是默认且唯一支持的 Lua 运行时。
JIT 编译是怎么工作的 LuaJIT 的 JIT 编译器叫 tracing JIT。它的工作方式是：
先用解释器执行代码，同时记录执行路径（trace） 当某条路径被执行超过阈值（默认 56 次），触发编译 把这条热路径编译成优化过的机器码 后续执行直接走机器码，跳过解释阶段 这意味着 JIT 的收益主要体现在循环和频繁调用的函数上。短命的代码（只执行一两次就结束）JIT 来不及发挥。
查看 JIT 编译状态 LuaJIT 提供了 jit 库可以控制和查看 JIT 状态：
-- 查看 JIT 是否开启 print(jit.status()) -- true / false -- 关闭 JIT（调试用） jit.off() -- 开启 JIT jit.on() -- 针对特定函数关闭 JIT jit.off(some_function) 在 OpenResty 里，可以用 luajit -jdump 工具查看哪些代码被编译了（生产环境一般不用，开发调试时有用）。
FFI：直接调用 C 库 FFI（Foreign Function Interface）是 LuaJIT 最强大的特性之一。它让你可以直接在 Lua 代码里声明和调用 C 函数，不需要写 C 扩展模块。
基本用 …</content></entry><entry><title>07 | Lua 模块系统与代码组织</title><url>/blog/openresty-07-lua-modules/</url><categories><category>OpenResty</category></categories><tags><tag>OpenResty</tag><tag>Lua</tag><tag>模块</tag></tags><content type="html"><![CDATA[  上一篇我们把 table 和元表研究透了。现在代码量开始多起来，总不能把所有逻辑都塞进一个文件。这篇来聊 Lua 的模块系统——如何拆分代码、如何复用，以及在 OpenResty 里该怎么组织项目结构。
require 是什么 Lua 用 require 加载模块，用法很简单：
local cjson = require &amp;#34;cjson&amp;#34; local redis = require &amp;#34;resty.redis&amp;#34; local my_util = require &amp;#34;utils.string_helper&amp;#34; require 背后做了几件事：
检查 package.loaded 缓存，如果已经加载过，直接返回缓存结果（不会重复执行） 在 package.path 指定的路径里查找对应的 .lua 文件 找到后执行该文件，把返回值存入缓存并返回 这个缓存机制很重要：同一个模块无论 require 多少次，只执行一次。
定义一个模块 Lua 模块本质上就是一个返回 table 的文件：
-- utils/math_helper.lua local M = {} function M.add(a, b) return a + b end function M.clamp(val, min, max) if val &amp;lt; min then return min end if val &amp;gt; max then return max end return val end -- 私有函数，不挂在 M 上，外部访问不到 local function internal_check(val) return type(val) == &amp;#34;number&amp;#34; end function M.safe_sqrt(val) if not internal_check(val) then return nil, &amp;#34;not a number&amp;#34; end return math.sqrt(val) end return M 使用时：
local math_helper = require &amp;#34;utils.math_helper&amp;#34; print(math_helper.add(3, 4)) -- 7 …  ]]></content></entry><entry><title>06 | Lua Table 深度解析</title><url>/blog/openresty-06-lua-table/</url><categories><category>OpenResty</category></categories><tags><tag>OpenResty</tag><tag>Lua</tag><tag>table</tag><tag>元表</tag></tags><content type="html"><![CDATA[  在前两篇文章里，我们把 Lua 的数据类型和流程控制都梳理了一遍。但有一个类型只是顺带提了一下——table。
这篇文章专门来聊它，因为 table 在 Lua 里的地位实在特殊：它是 Lua 里唯一的数据结构。数组是它，字典是它，模块是它，对象也是它。搞懂 table，相当于搞懂了 Lua 的半壁江山。
table 的双重身份 Lua 里没有内置的&amp;quot;数组类型&amp;quot;或&amp;quot;字典类型&amp;quot;，只有一个 table，但它能同时扮演这两个角色。
作为数组 local fruits = {&amp;#34;apple&amp;#34;, &amp;#34;banana&amp;#34;, &amp;#34;orange&amp;#34;} print(fruits[1]) -- apple（注意：Lua 数组下标从 1 开始！） print(fruits[2]) -- banana print(#fruits) -- 3（# 获取长度） -- 遍历数组 for i, v in ipairs(fruits) do print(i, v) end 注意那个 [1]——Lua 的数组下标从 1 开始，不是 0。这是很多从 C / Python / JavaScript 转过来的开发者最先踩的坑。
ipairs 是专门用来遍历数组部分的迭代器，它从下标 1 开始，遇到第一个 nil 就停下来。
作为字典 local config = { host = &amp;#34;127.0.0.1&amp;#34;, port = 6379, timeout = 1000, } print(config[&amp;#34;host&amp;#34;]) -- 127.0.0.1 print(config.port) -- 6379（两种写法等价） -- 遍历字典 for k, v in pairs(config) do print(k, v) end pairs 遍历 table 里所有的键值对，包括数组部分和字典部分，但顺序不固定。
同时作为两种 table 完全可以既有数组部分又有字典部分：
local user = { &amp;#34;alice&amp;#34;, -- [1] &amp;#34;bob&amp;#34;, -- [2] name = &amp;#34;alice&amp;#34;, age = 28, } print(user[1]) -- alice …  ]]></content></entry><entry><title>05 | Lua 流程控制与函数</title><url>/blog/openresty-05-lua-control-flow/</url><categories><category>OpenResty</category></categories><tags><tag>OpenResty</tag><tag>Lua</tag></tags><content type="html"><![CDATA[  上一篇我们把 Lua 的六种数据类型摸了个遍。有了数据，下一步自然是：怎么用代码控制这些数据的流向？
这篇文章讲 Lua 的流程控制和函数系统。内容不多，但有几个地方和主流语言不太一样，如果不注意，写出来的代码虽然能跑，但很可能埋着隐患。
if / elseif / else：条件判断 Lua 的 if 语法和大多数语言差不多，但有两点值得注意：用 then 和 end 包围，不用花括号。
local score = 85 if score &amp;gt;= 90 then print(&amp;#34;优秀&amp;#34;) elseif score &amp;gt;= 60 then print(&amp;#34;及格&amp;#34;) else print(&amp;#34;不及格&amp;#34;) end 多条件分支用 elseif（注意是一个词，不是 else if）。
在 OpenResty 里，条件判断随处可见，比如根据请求方法分流处理：
local method = ngx.req.get_method() if method == &amp;#34;GET&amp;#34; then handle_get() elseif method == &amp;#34;POST&amp;#34; then ngx.req.read_body() handle_post() else ngx.exit(405) -- Method Not Allowed end 回忆上一篇说的：只有 nil 和 false 是假。所以判断一个值是否存在，直接 if val then 就够了，不需要写 if val ~= nil then（虽然两者等价）：
local token = ngx.req.get_headers()[&amp;#34;Authorization&amp;#34;] if token then -- token 存在，继续处理 else ngx.exit(401) end for 循环：两种完全不同的写法 Lua 的 for 有两种形式，用途完全不同，不要混用。
数值 for（numeric for） 适合按数字步长遍历：
-- 从 1 到 5，步长 1 for i = 1, 5 do print(i) -- 1 2 3 4 5 end -- 从 10 到 1，步长 -2（倒序） for i = 10, 1, -2 do print(i) -- 10 …  ]]></content></entry><entry><title>04 | Lua 数据类型与变量</title><url>/blog/openresty-04-lua-types/</url><categories><category>OpenResty</category></categories><tags><tag>OpenResty</tag><tag>Lua</tag></tags><content type="html"><![CDATA[  上一篇我们搞清楚了 OpenResty 的执行生命周期，知道了每个阶段能做什么事情。从这篇开始，我们进入第二阶段：Lua 语言基础。
很多人问：学 OpenResty 必须先精通 Lua 吗？答案是不必要。Lua 是一门很小的语言，核心语法十分钟就能扫完。但有几个地方如果不搞清楚，后面写代码会踩很多坑。这篇文章就专门讲这些基础但重要的东西。
Lua 只有六种数据类型 很多编程语言有几十种数据类型，而 Lua 只有六种：
类型 说明 nil 空值，表示&amp;quot;不存在&amp;quot; boolean 布尔值，true 或 false number 数字（整数和浮点数统一用这个） string 字符串 table 表，Lua 最核心的数据结构 function 函数也是一种值 这六种类型就是 Lua 的全部。简单是 Lua 的设计哲学，也是它能嵌入 OpenResty 这种高性能系统的原因之一。
用 type() 函数可以查看一个值的类型，返回的是类型名称字符串：
print(type(nil)) -- nil print(type(true)) -- boolean print(type(42)) -- number print(type(3.14)) -- number print(type(&amp;#34;hello&amp;#34;)) -- string print(type({})) -- table print(type(print)) -- function 记住这个函数，后面判断类型时会经常用到。
nil：不存在就是 nil nil 在 Lua 里表示&amp;quot;什么都没有&amp;quot;。一个变量如果没有赋值，它的值就是 nil；把一个变量设置为 nil，相当于删除这个变量。
local x print(x) -- nil，未赋值的变量默认是 nil local t = {a = 1, b = 2} t.b = nil -- 把 t.b 从 table 里删掉 print(t.b) -- nil 在 OpenResty 里，nil 经常出现在&amp;quot;取不到值&amp;quot;的场景。比如从 Redis 查一个不存在的 key，返回的就是 nil：
local res, err = red:get(&amp;#34;nonexistent_key&amp;#34;) if res == …  ]]></content></entry><entry><title>03 | OpenResty 执行生命周期</title><url>/blog/openresty-03-lifecycle/</url><categories><category>OpenResty</category></categories><tags><tag>OpenResty</tag><tag>Nginx</tag><tag>Lua</tag></tags><content type="html"><![CDATA[  上一篇我们搭好了环境，跑通了第一个 Hello World。但当时埋了一个伏笔——content_by_lua_block 里的 content 到底是什么意思？为什么不叫 run_lua_block 或者 exec_lua_block？
这篇文章就来解答这个问题。搞懂 OpenResty 的执行生命周期，是真正理解 OpenResty 编程模型的关键一步。
从一个比喻开始 把处理一个 HTTP 请求想象成在餐厅点餐的全过程。
顾客（客户端）走进餐厅，服务员（Nginx）接待。整个流程大致是：门口迎宾检查着装 → 带位 → 点单 → 后厨做菜 → 上菜 → 结账离开 → 收拾桌子写日志。
每个环节都有自己的职责。迎宾不负责炒菜，后厨不负责结账，收银员不管收拾桌子。如果你在&amp;quot;带位&amp;quot;阶段突然要求&amp;quot;炒一道菜给我&amp;quot;，服务员会一脸懵——这不是你该在这个阶段提的要求。
Nginx 处理请求也是这个逻辑，它把一次请求的生命周期切成了若干个阶段，每个阶段有明确的职责，不能随意跨阶段操作。
Nginx 请求处理的 11 个阶段 Nginx 内部将 HTTP 请求处理划分为以下 11 个阶段：
POST_READ → 读取请求头之后，最早能介入的阶段 SERVER_REWRITE → server 块级别的 rewrite FIND_CONFIG → 查找匹配的 location 配置 REWRITE → location 块内的 rewrite POST_REWRITE → rewrite 完成后的收尾 PREACCESS → access 之前的限流、限速等 ACCESS → 权限检查（认证、鉴权） POST_ACCESS → access 结果处理 PRECONTENT → 内容生成前的准备（try_files 在这里） CONTENT → 生成响应内容（核心阶段） LOG → 请求结束后记录日志 OpenResty 在这 11 个阶段的基础上，提供了一系列 *_by_lua* 指令，让你可以在对应阶段嵌入 Lua 代码。常用的对应关系如下：
init_by_lua_block → worker 启动时执行（全局初始化） init_worker_by_lua_block → 每个 worker …  ]]></content></entry><entry><title>02 | 环境搭建与目录结构</title><url>/blog/openresty-02-setup/</url><categories><category>OpenResty</category></categories><tags><tag>OpenResty</tag><tag>Nginx</tag><tag>Lua</tag><tag>环境搭建</tag></tags><content type="html"> 上一篇我们搞清楚了 OpenResty 是什么。从这篇开始，就要真正动手了。
工欲善其事，必先利其器。这篇文章的目标很简单：在你的机器上把 OpenResty 跑起来，理解它的目录结构，然后写出第一段能运行的 Lua 代码。
两种安装方式 OpenResty 的安装主要有两条路：包管理器安装和源码编译。对于学习阶段来说，包管理器安装就够了，速度快、省心。源码编译适合生产环境需要定制模块的场景，这篇先不展开。
另外还有一种方式是 Docker，适合不想污染本机环境、或者系统不方便装软件的情况，后面单独讲。
用包管理器安装（推荐） OpenResty 官方提供了预编译的包，支持主流 Linux 发行版。
Ubuntu / Debian # 安装依赖 sudo apt-get install -y lsb-release curl # 添加 OpenResty 官方 APT 源 curl -fsSL https://openresty.org/package/pubkey.gpg | sudo gpg --dearmor -o /usr/share/keyrings/openresty.gpg echo &amp;amp;#34;deb [arch=$(dpkg --print-architecture) signed-by=/usr/share/keyrings/openresty.gpg] http://openresty.org/package/ubuntu $(lsb_release -sc) main&amp;amp;#34; \ | sudo tee /etc/apt/sources.list.d/openresty.list &amp;amp;gt; /dev/null # 更新并安装 sudo apt-get update sudo apt-get install -y openresty CentOS / RHEL # 添加 OpenResty YUM 源 sudo yum install -y yum-utils sudo yum-config-manager --add-repo https://openresty.org/package/centos/openresty.repo # 安装 sudo yum install -y openresty 安装完成后，验证一下是否成功： …</content></entry><entry><title>慢下来，才能感觉到</title><url>/life/slow-down-and-feel/</url><categories><category>生活点滴</category></categories><tags><tag>随笔</tag><tag>感悟</tag></tags><content type="html"><![CDATA[  今天早上出门，比平时晚了十分钟。
不是闹钟没响，也不是赖床，就是莫名其妙地在窗边站了一会儿，看着楼下的早市收摊，看着送外卖的小哥把车停在路边，低头翻着手机确认地址，看着对面楼的窗户一扇一扇亮起来。
就站了大概五分钟，什么都没做。
结果发现，那五分钟比我过去很多个早上都要清醒。
我不太确定从什么时候开始，生活变成了一种需要&quot;完成&quot;的事情。
起床、洗漱、吃饭、上班，每一步都是在和时间赛跑。手机里永远有没读完的消息，待办清单永远比能做完的事情多。哪怕是下班之后的休息，也常常是带着某种隐隐的焦虑——&ldquo;我是不是应该学点什么&rdquo;、&ldquo;这段时间是不是虚度了&rdquo;。
效率成了一种习惯，甚至变成了一种道德绑架。
不够快，不够满，不够有用，就会觉得不安。
但人不是机器。
机器运转是为了输出，跑得越快越好，越稳越好。人活着却不只是为了输出。那些在等公交时随手拍的一张光影，和老朋友聊到半夜的一段废话，一个人走路时脑子里突然冒出来的奇怪念头——这些东西没有 KPI，没有回报，但它们才是生活里真正有质地的部分。
很多东西只有慢下来才能感觉到。
快步走过一条街，你记住的是目的地。慢慢走一遍，你会注意到拐角那家饺子馆的幌子已经褪色了，会注意到路边有棵树不知道什么时候开始长了一块奇怪的疤，会注意到有一户人家的窗台上摆着一盆长歪了的多肉，歪得很有个性。
这些细节没什么用。但它们让你觉得你真实地存在过，在那个时间，在那个地方。
我没有什么解决办法，也不打算给&quot;慢生活&quot;唱赞歌——那听起来像是一种消费主义包装出来的人设。
我只是想记住今天早上那五分钟。
窗外的光是斜的，还带着点冬末的凉意。楼下有人在喊&quot;豆腐脑，豆腐脑&quot;，声音拉得很长，懒洋洋的。
什么都没发生，但好像什么都有了。
  ]]></content></entry><entry><title>01 | OpenResty 是什么</title><url>/blog/openresty-01-what-is-openresty/</url><categories><category>OpenResty</category></categories><tags><tag>OpenResty</tag><tag>Nginx</tag><tag>Lua</tag></tags><content type="html"><![CDATA[  如果你刚刚接触 OpenResty，可能会有一个疑问：这东西到底是什么？是服务器？是框架？还是某种编程语言？
这篇文章的目的就是帮你从零建立对 OpenResty 的基本认知，不需要任何技术背景，读完之后你就能清楚地说出&amp;quot;OpenResty 是什么、能做什么、适合什么场景&amp;quot;。
从一个生活场景说起 想象一下你去一家大型商场。商场的大门有一个保安，负责检查每一个进来的人：有没有携带危险物品、是不是会员、今天有没有特别活动限制入场。
但这个保安不是商场自己设计的那种&amp;quot;只能说你好欢迎光临&amp;quot;的普通保安，而是一个经过特殊培训的&amp;quot;智能保安&amp;quot;——他可以查数据库、打电话确认身份、根据当天的规则灵活处理各种情况，处理完之后再决定放不放行、放行去哪里。
在互联网世界里，用户发出的每一个请求（比如打开网页、调用 APP 的接口）都需要经过类似的&amp;quot;入口检查&amp;quot;。OpenResty 做的就是这个&amp;quot;智能入口&amp;quot;的角色。
Nginx 是什么 要理解 OpenResty，必须先了解 Nginx（读作 engine-x）。
Nginx 是目前全球使用最广泛的 Web 服务器之一。你访问的大量网站背后都跑着 Nginx。它的核心工作是：接收用户发来的请求，然后决定把这个请求转发给谁、或者直接返回什么内容。
Nginx 非常擅长同时处理大量请求。普通服务器可能同时处理几百个请求就吃力了，而 Nginx 能轻松应对几万甚至几十万个并发请求，而且内存占用极低。这是它最核心的优势。
但 Nginx 有一个明显的局限：它本身的逻辑是固定的，如果你想在请求处理过程中加入自定义的业务逻辑，比如&amp;quot;检查一下这个用户今天是否已经请求超过 100 次&amp;quot;，原生 Nginx 做起来非常麻烦，需要用 C 语言写扩展模块，难度极高。
OpenResty 在 Nginx 的基础上做了什么 OpenResty 的核心思路很简单：在 Nginx 里内置了一个叫 LuaJIT 的脚本语言执行引擎，让开发者可以用 Lua 这门脚本语言，在请求处理的各个环节插入自定义逻辑。
这就像给那个&amp;quot;普通保安&amp;quot;配备了一台可以随时编程的平板电脑，保安可以根据实时写入的规则来处理各种情况，而不是只会执行固定的几条指令。
Lua …  ]]></content></entry><entry><title>关于我</title><url>/about.html</url><categories/><tags/><content type="html"> 你好，我是红岩。
一个在北京的后端开发者，平时主要做服务端开发和网关相关的工作，偶尔也会涉猎其他技术方向。
关于这个博客 这里是我记录和分享的地方。
写博客没有太宏大的目标，就是想把学过的东西整理出来，把踩过的坑记下来，顺带把生活里觉得值得记一笔的事情也留下来。
技术文章为主，生活随笔为辅。能对看到这里的人有一点点帮助，就够了。
关于我这个人 工作之外，喜欢听歌、唱歌，偶尔看电影。
不是那种把所有时间都扑在技术上的人，觉得生活和代码一样需要认真对待。
联系方式 GitHub： github.com/zanehy 如果文章里有什么写得不对的地方，欢迎指出。</content></entry></search>